# Implementation Status

**This document records reality, never intent.** It is the authoritative answer to "what state is this in?" — no other document should assert build state.

**Project phase: IMPLEMENTATION.** The V1 specification is complete; implementation is **authorized** (`IMPL-01`, 2026-08-17) and underway.

Documents under [`09-implementation/`](../09-implementation/) remain *specifications of a target*. Where code and specification disagree, the specification wins — `IMPL-01` condition 1: *the code is not a specification.*

Last synchronized against `all_lock.md` at **19,374 lines** (2026-09-13 — **AB-20 `AM-59`–`AM-62`, the Constitution-driven, content-first review**: the Legal Constitution **L1.10** governs (canonical, redacted; L1.5 superseded; no number in §§9–22 changed; `AUTORENEW-MSA-001` reconciled to §31.15's 30-day non-renewal notice); every standard carries a `constitution` block (section, Appendix B topic, basis, `expected_when`), validated at publish and import; the semantic stage runs for every pinned requirement (the `AM-54` r4 gate removed on a live four-mode corpus measurement — 0 semantic FP / 38 negatives, 0 changed-correct in every mode); applicability records a reason per requirement (APPLIED · NOT_APPLICABLE · SAME_POSITION) in the analysis audit event and the report; one Constitution position is measured once; established absence on a numeric requirement is MISSING, not a dropped Finding; declared, definitional unit conversion (YEARS↔MONTHS, WEEKS↔DAYS); ten proposed standards drafted under `company_standards/proposed/` (read by nothing until ratified); then the owner's final business decisions of 2026-09-14 (`AM-65` — the seven standards the Constitution does not define retired via `ConfigStatus.DEPRECATED`, 184 of 580 live Findings affected, history preserved, zero Legal Decisions invalidated; `AM-66` — service discontinuation as one compound requirement; the Tier-2 gate now refuses a pipeline-identity mismatch and was re-baselined under the shipped pipeline, recall@10 0.438 → 0.625 with wrong answers unchanged at 0 reaching a user; retention (41.26) answered from Constitution §26.1 so preflight moves BLOCKED → PASS; `tools/verify_terminology` joined CI; documentation set rewritten) and, on 2026-09-13, (`AM-59` r6'/r9 approval through the Constitution — 7 standards ratified, 39 in all; `AM-62` r5 phrase-anchored quantity; `AM-63` §24.4 Not-Negotiable → Needs a decision, C-20 resolved; `AM-64` no reader type selection, C-21 moot). Verified: 1,605 backend and 398 frontend tests, ruff/mypy clean; real OTHER contract and real mixed document re-analysed in rolled-back transactions (21 distinct positions instead of 15 + 6 duplicates; 7 → 8 findings with 0 duplicates and 23 recorded NOT_APPLICABLE). **BUILT in a worktree, NOT deployed** — the standards need re-import + publish. Previously **18,499 lines** (2026-09-13 — **"Not recorded" precision fix**, no lock amended: a fail-closed NUMERIC result now reports the company standard's own position, and a value cell distinguishes "Not recorded" / "Not found" / "Stated, but not readable", showing the figure actually read when the refusal is about comparability. Verified on the reported contract by re-running the real analysis in a rolled-back transaction: classifications byte-identical (15 MATCH, 1 MISSING, 5 UNABLE_TO_EVALUATE), zero rows blank on both sides; 1,588 backend and 392 frontend tests, reproducibility gate identical digest. Two owner decisions raised and NOT taken: widening the `AM-54` semantic gate beyond the declared family, and extraction vocabulary/basis synonyms (rule 7). BUILT in a worktree, NOT deployed — see CHANGELOG) (2026-09-11 — **AB-19 / `AM-58`**, bounded conversation context in a generation payload, amending `AM-30` t2 narrowly; see the registry). Previously **18,433 lines** (2026-09-10 — **P0 backend quality phase**, no lock amended: chunker `clause-aware-4` (structure-based folding, page furniture excluded, heading rows redirected at query time), id-preserving re-index `tools.reindex_documents` (live dry run 8,065 → 7,371 chunks, 32.7% → 25.5% under 80 chars, 136 → 136 citations), Ask conversation memory inside `AM-30` t2 (prior USER questions only, `grounded-answer-2`), source routing re-audited with the A–E matrix pinned; Tier-2 gate SHIPPABLE before and after; **DEPLOYED 2026-09-11** — merged `ec32467`, API restarted, live index re-indexed 8,065 → 7,371 chunks with 170/170 document citations preserved, 18/18 post-deployment smoke checks passed; hit@1 0.391 → 0.375 traced to one question caused by the query-time redirect, every gated quantity held — see CHANGELOG) (2026-09-10 — **workspace fix pass, DD-17**, no lock amended: adaptive pane widths with a 400px side-card floor, the Contents as a presentation-layer tree printing the document's own numbers (no `§`; clauses the parser's comma rule missed are admitted, party-block connectives dropped — parser untouched), per-group "Show N more" for obligations, colliding finding titles qualified by each standard's description, Ask taking the side column while open, the ratified position quoted beside a document answer with the evaluator's existing Finding as its assessment (`finding.view` gated, read never produced); BUILT and TESTED in a worktree, NOT deployed — see CHANGELOG) (2026-09-09 — **AB-18 `AM-57`**: the workspace is document-primary — LEFT contents index · CENTRE document · RIGHT findings/Ask, amending `AM-50` r5 and superseding DD-15's floating Ask dock; the contents index derives clause numbers from the evidence row's own text where the parser recorded none (presentation layer only — the parser is NOT retuned and evidence is NOT re-extracted, `AM-57` r3); Ask docks into the right column; fewer nested frames; Lucide throughout. **BUILT and TESTED locally, NOT deployed.** No evaluator, classification, Rule Outcome, API, permission or database change) (2026-09-10 — **Ask retrieval architecture fix**, no lock amended: authorization-derived fallback sources consulted before any refusal from every non-answer cause; AND-gated, OR-candidate document retrieval with heading fragments pruned; Domain A vectors filled and hybrid; Domain C rank-fused for unnamed questions; a verdict screen for generated text; Tier-2 gate SHIPPABLE — wrongly answered 1/13, recall@10 0.625, faithfulness 1.0; BUILT, TESTED (1489 passed), live-verified in-process, NOT deployed — see CHANGELOG) (2026-09-09 — `AM-56`: the three words are Acceptable / Requires modification / Needs a decision, mapped from the five determinations by classification; Summary shows exactly three tiles) (2026-09-09 — **AM-53 and AM-54 DEPLOYED**: migration `a1b2c3d4e5f6` at head, API restarted, frontend build `SNZfKgUQCnzqvohtgOIYv`, `LIABILITY-*-001` version 3 imported and a new snapshot published, live paraphrased-contract review verified through the production API) (2026-09-09 — `AM-54` correction r9–r13 after the live labelled corpus: SAME-position check, semantic mapping never establishes absence, table rows are not headings, floor 0.30; 104 variants / 19 standards: 66/66 recognised, 0 semantic FP, 0 changed-correct) (2026-09-09 — `AM-54`: grounded semantic recognition — `analysis/semantic.py`, two stages (in-family mapping adjudicated on a verbatim span; requirement-aware quantity reading verified in the span), number words read as numerals, `LIABILITY-*-001` gained the L-13 negative patterns (re-import + publish required to reach the live DB); BUILT and TESTED locally against three materially different drafting styles with the real model, NOT deployed) (2026-09-09 — `AM-53`: the FINAL three-status model — `user_status` derived server-side in `evaluation/user_status.py` and served on every Finding/Evaluation, report, Dashboard list, comparison and export; BUILT and TESTED locally, NOT deployed) (2026-09-09 — `AM-52`: a mapped clause stating no cap keeps its evidence; UNABLE_TO_EVALUATE reads "Needs Review"; Domain A rechunked onto the published versions) (2026-09-09 — an `AM-50` r3 correction: statute title-ranking is a lexeme ratio excluding only india/indian, found and fixed during pre-deployment live verification) (2026-09-09 — an `AM-51` r2 correction: family detection is the declared type only, found and fixed during pre-deployment live verification) (2026-09-09 — **AB-16 `AM-50`/`AM-51`**: applicability by content, no document-type gate anywhere; Upload → Review → Ask — intake records a confident type (audited), Ask falls through to statutes/positions, Summary in the reader's three words, findings-first layout, "View details"; BUILT and TESTED locally, NOT deployed; **AB-15 `AM-49`**: the grounded explanation layer — `assist/explanations.py`, `POST /findings/{id}/explain`, migration `f3a9c2d7e1b4` `finding_explanations`, **BUILT and TESTED locally, NOT deployed, live DB NOT migrated**; 2026-09-08 — AB-14 `AM-43`–`AM-48`: the Constitution governs, the Ask router, Domain A wired, Domain C built and its corpus completed from India Code; see unit 12). *Earlier sync line, retained:* **16,689 lines** (Steps 1–45D, 47, 49, 52–55, `REC-01`–`REC-09`, Amendment Batches AB-1 through **AB-7** (`AM-34`/`AM-35`, suggestion-assisted intake + Key Obligations, 2026-08-31), the AM-31 gate release, Implementation Authorization, `DOC-06`/`DOC-07`). **AM-34/AM-35 build state (2026-08-31): BUILT and TESTED** — `assist/type_suggestion.py` + `POST /document-versions/{id}/suggest-type`; migration `e5b8d3f17a2c` (two assist-schema tables), `assist/obligations.py` + extract/read endpoints behind `finding.view`, judgment-language guardrail; upload-first intake, sticky Ask bar, AI Analysis panel. **AM-32 build state (2026-08-27):** migration `d7e2a9c41b58` (five tables + `answer_citations` per-domain FK columns, round-tripped), `legalmind/assist/positions.py` (Domain A chunking + permission-gated lexical search, 9 tests), `tools/chunk_standards.py`. Not yet built: Domain A conversation-API wiring and UI pane, vector branch (decision #177), the r9 Tier-2 Domain A/C questions (gate before shipping), everything Domain C (blocked on C-16 material). (2026-09-09 — **AM-49 and AM-53–AM-56 DEPLOYED**: main `96103ea`, frontend build `EwrTKT7NlZdJDmg-CHU4Q`, API restarted, **no migration ran** — the live database was already at `a1b2c3d4e5f6`, so `f3a9c2d7e1b4` had been applied before this deploy and the standing note that a migration was owed is superseded; verified as SERVED, the login HTML carrying the new BUILD_ID and the deployed stylesheet carrying `--ws-tone-warn:#c2410c` / `--ws-tone-bad:#4f46e5`. The 72 commits merged here had never been through CI: 56 ruff findings, 8 mypy errors, and three tests that were GREEN WHILE PROVING NOTHING — the LEGAL-02 confidentiality spec, the prompt-injection guardrail test, and a skip guard that raised. All fixed in the same PR. **NOT deployed**: `AM-57` and the Ask retrieval fix) (2026-09-09 — `AM-56`: the three words are Acceptable / Requires modification / Needs a decision, mapped from the five determinations by classification; Summary shows exactly three tiles) (2026-09-09 — **AM-53 and AM-54 DEPLOYED**: migration `a1b2c3d4e5f6` at head, API restarted, frontend build `SNZfKgUQCnzqvohtgOIYv`, `LIABILITY-*-001` version 3 imported and a new snapshot published, live paraphrased-contract review verified through the production API) (2026-09-09 — `AM-54` correction r9–r13 after the live labelled corpus: SAME-position check, semantic mapping never establishes absence, table rows are not headings, floor 0.30; 104 variants / 19 standards: 66/66 recognised, 0 semantic FP, 0 changed-correct) (2026-09-09 — `AM-54`: grounded semantic recognition — `analysis/semantic.py`, two stages (in-family mapping adjudicated on a verbatim span; requirement-aware quantity reading verified in the span), number words read as numerals, `LIABILITY-*-001` gained the L-13 negative patterns (re-import + publish required to reach the live DB); BUILT and TESTED locally against three materially different drafting styles with the real model, NOT deployed) (2026-09-09 — `AM-53`: the FINAL three-status model — `user_status` derived server-side in `evaluation/user_status.py` and served on every Finding/Evaluation, report, Dashboard list, comparison and export; BUILT and TESTED locally, NOT deployed) (2026-09-09 — `AM-52`: a mapped clause stating no cap keeps its evidence; UNABLE_TO_EVALUATE reads "Needs Review"; Domain A rechunked onto the published versions) (2026-09-09 — an `AM-50` r3 correction: statute title-ranking is a lexeme ratio excluding only india/indian, found and fixed during pre-deployment live verification) (2026-09-09 — an `AM-51` r2 correction: family detection is the declared type only, found and fixed during pre-deployment live verification) (2026-09-09 — **AB-16 `AM-50`/`AM-51`**: applicability by content, no document-type gate anywhere; Upload → Review → Ask — intake records a confident type (audited), Ask falls through to statutes/positions, Summary in the reader's three words, findings-first layout, "View details"; BUILT and TESTED locally, NOT deployed; **AB-15 `AM-49`**: the grounded explanation layer — `assist/explanations.py`, `POST /findings/{id}/explain`, migration `f3a9c2d7e1b4` `finding_explanations`, **BUILT and TESTED locally, NOT deployed, live DB NOT migrated**; 2026-09-08 — AB-14 `AM-43`–`AM-48`: the Constitution governs, the Ask router, Domain A wired, Domain C built and its corpus completed from India Code; see unit 12). *Earlier sync line, retained:* **16,689 lines** (Steps 1–45D, 47, 49, 52–55, `REC-01`–`REC-09`, Amendment Batches AB-1 through **AB-7** (`AM-34`/`AM-35`, suggestion-assisted intake + Key Obligations, 2026-08-31), the AM-31 gate release, Implementation Authorization, `DOC-06`/`DOC-07`). **AM-34/AM-35 build state (2026-08-31): BUILT and TESTED** — `assist/type_suggestion.py` + `POST /document-versions/{id}/suggest-type`; migration `e5b8d3f17a2c` (two assist-schema tables), `assist/obligations.py` + extract/read endpoints behind `finding.view`, judgment-language guardrail; upload-first intake, sticky Ask bar, AI Analysis panel. **AM-32 build state (2026-08-27):** migration `d7e2a9c41b58` (five tables + `answer_citations` per-domain FK columns, round-tripped), `legalmind/assist/positions.py` (Domain A chunking + permission-gated lexical search, 9 tests), `tools/chunk_standards.py`. Not yet built: Domain A conversation-API wiring and UI pane, vector branch (decision #177), the r9 Tier-2 Domain A/C questions (gate before shipping), everything Domain C (blocked on C-16 material).

---

## Build state

**Authorized 2026-08-17** (`IMPL-01`), recorded retroactively and not backdated: the build preceded the authorization and the lock record says so.

### Release state — AB-22 and AB-23 DEPLOYED (2026-09-16)

| Work | Merged as | Build state |
|---|---|---|
| `AM-70` — `DELETE /counterparties/{id}` for a Client Profile with no documents | PR #54 `10113e0` | **IMPLEMENTED · TESTED · DEPLOYED** |
| `AM-71` — Ask retrieval excludes a `DEPRECATED` Requirement (lexical **and** vector) | PR #55 `6c8af71` | **IMPLEMENTED · TESTED · DEPLOYED** |

No migration was required by either. `legalmind-api`, `legalmind-worker` and
`legalmind-frontend` restarted clean — `NRestarts=0` on each — and the deploy tree is at
`6c8af71`, equal to `origin/main`.

**`AM-71` was verified against live production data, not only in tests.** Six probes through
the deployed `search_positions` returned **zero** retired standards while active standards
returned normally. The counterfactual matters more than the pass: the same lexical query with
the filter removed ranks `FORCE-MAJEURE-TOS-001` (`DEPRECATED`) **first** and
`FORCE-MAJEURE-MSA-001` fourth. The defect was live, not theoretical.

`position_chunks` still holds **40 rows — 33 `ACTIVE` + 7 `DEPRECATED`**, deliberately: the
ruling preserves superseded positions for explicit version, history and audit use, so the
exclusion is read-side. Retirement removes a position from retrieval; it does not erase it.

Each of the three new tests in `tests/test_positions.py` was confirmed to FAIL with the filter
removed (2 failed / 16 passed disabled; 18 passed restored). CI on the merge head: **27 checks
pass, 3 skip, 0 fail**, including job 13 (whole suite) and job 10 (browser workflows).

⚠️ **One CI attempt on PR #54 failed job 10 and was not a regression.** The API process died
mid-run (`ECONNRESET` at 06:45:19, no traceback) after 104 tests had passed; the 23 subsequent
`POST /contracts failed: 500` are the frontend proxy reporting a dead socket. The same commit
passed job 10 on another attempt, and PR #55 passed it. This is the recorded nondeterministic
job-10 flake — it was diagnosed from the log, not assumed, and no retry was added to mask it.

### Release state — Ask AI safety and citation fixes BUILT, NOT DEPLOYED (2026-09-15, later)

Supersedes nothing below; it records work that exists only on unmerged branches.

⚠️ **SUPERSEDED 2026-09-16 — this work is now MERGED and DEPLOYED.** It landed as PR #53
(`ad025f1`) and reached the running system with the 2026-09-16 deploys recorded above. The
three "still carries every defect" statements below were true when written and are no longer
true. The corpus **has** been re-chunked: `position_chunks` was rebuilt 2026-09-16 10:26 and
**0 of its 40 rows contain a repository path or filename**, so the sanitizer's write-time fix
is in force in production. The verification gap below is also closed — the DB-backed suite
now runs (CI job 13 green on both merge heads). Left unedited below as the record of what was
true on 2026-09-15.

**The deploy tree still carries every defect named here.** Verified 2026-09-15 against
`/root/Legalmind.v1`: `guardrails.py` still has the `else 1.0` grounding default,
`intent.py` still has `_WORD = re.compile(r"[a-z]+")`, and `positions.py` still composes the
raw `source_document`. Nothing below is merged, deployed, or reflected in the running system.

| Work | Branch | Build state |
|---|---|---|
| Language safety screens (`AM-25` r4/r5 enforced in Hindi and Hinglish) | `fix/language-safety-screens` `d3b9985` | **IMPLEMENTED**, unit-TESTED, **not deployed** |
| Domain A source-locator sanitizer | `fix/ask-source-leak` `f5fc324` | **IMPLEMENTED**, TESTED, **not deployed**, **corpus not re-chunked** |
| Position citation version + ratification status | `fix/ask-source-leak` `c82f5ff` | **IMPLEMENTED**, unit-TESTED, **not deployed** |

⚠️ **The sanitizer is a write-time fix and the corpus has not been re-chunked**, so
`position_chunks` still serves the leaked text. The defect is closed in code and **open in
the running system**.

⚠️ **VERIFICATION GAP.** No DB-backed test has run against this work: no worktree has
`LEGALMIND_TEST_DATABASE_URL`. Full backend suite = **945 passed · 115 skipped · 1 xfailed ·
907 errors · 0 FAILED**, every error being `password authentication failed` at fixture setup.
The 77-question release gate (`verify_assist_quality.py`) could not run, so **no claim is made
that wrongly-answered has not risen**. Frontend: 474 passed across 34 files.

Evidence per command: [ASK_AI_PROGRAMME.md](ASK_AI_PROGRAMME.md) §D.

**Ask pipeline build state, stage by stage:** the owner's ten canonical stages and what is shipped / built-but-off / not built are tabulated in [ASK_TARGET_ARCHITECTURE.md](ASK_TARGET_ARCHITECTURE.md) §0. Summarised here because this document is the only one permitted to assert build state, current as of the 2026-09-17 consolidation (PRs #74, #76, #78 merged and DEPLOYED at `cbc9563`):

- Stages 1, 3, 5, 7, 8, 9, 10 **IMPLEMENTED and DEPLOYED**.
- Stage 6 (cross-encoder reranker, `backend/legalmind/assist/rerank.py`) **MERGED, DEPLOYED and ENABLED** (`LEGALMIND_RERANK=on`, PR #74; verified in the running process 2026-09-17). `ms-marco-MiniLM-L-6-v2`, pinned + checksummed. Measured: hit@1 0.609→0.734, MRR 0.709→0.796, recall 0.891→0.906, +205ms, wrongly-answered and faithfulness unchanged. **Enabling it was a separate operator step**, taken deliberately apart from the code deploy so a misbehaviour afterwards attributes to one change, not two.
- **Stage 10 replay stopped leaking a withdrawn grant (PR #85, DEPLOYED 2026-09-17).** `GET /conversations/{id}` checked only that the caller created the conversation, then served the cited clause text and the verbatim Company Position that turn had quoted — so after `legal_position.view` was revoked, or `contract.transfer` moved the deal away, a reload still returned `standard_code`, `source_clause` and the position text, the disclosure `LEGAL-02` forbids. Replay now re-resolves the two tests the live path already applies per request (S-1): `routing.positions_permitted` and `can_read_contract`; withheld material is **omitted, not nulled** (`SEC-07`, `API-10`). `AM-25` r5 traceability is unchanged for callers who may still read the source. 13 tests; the 8 withholding cases run everywhere, the 2 that assert a document IS cited carry `needs_embedding_model`. Written and measured on `feat/p1-rag-quality` 2026-09-11 and unmerged until now; that branch's other three items (HNSW refused on evidence, grounding normalisation, redirect re-scoring) stay **PARKED** — each changes shipped retrieval or verification behaviour and needs re-measurement against a pipeline that now has the reranker on.
- Stage 10 gained citation markers as references (`AnswerProse`, PR #78) and the embedding model now warms at boot instead of on the first reader's question (same PR) — both **DEPLOYED**.
- Stages 2 and 4 (Gemini query understanding + query planner, `backend/legalmind/assist/planner.py`) **IMPLEMENTED, MEASURED, STILL UNMERGED** (PR #69, open) — no recall gain, +2.4–8 s latency, evidence precision down. Needs an owner decision (merge switched off, or park the code) before this cell changes again.

No stage has changed what the gate admits. `LEGALMIND_POSITION_SYNTHESIS` (`AM-67`) remains a separate, still-pending owner decision, untouched by this consolidation — the two flags must not share a restart.

### Release state — AB-20 COMPLETE: the 33 standards are published (2026-09-15)

Supersedes the sections below, which recorded earlier states accurately and stand as written.

**The Constitution Standards are live.** Imported 2026-09-15 11:09 and published 11:15:24 by
`aman.singh@leapswitch.com` through `POST /configuration/publish` (audited as
`config.published`). Snapshot **`5c85b87c`** carries **33 items, all ACTIVE**.

| | |
|---|---|
| Requirements | **40** — 33 `ACTIVE`, 0 `DRAFT`, **7 `DEPRECATED`** |
| Retired standards in the new snapshot | **0** — all seven correctly refused |
| Findings / reviews | **580 / 58 — unchanged**; 428 cite active standards, **152 cite the retired seven** and still resolve, reading "Retired — not present in the current Constitution" |
| Prior snapshots | untouched — 32 items each, 44 / 6 / 8 reviews still pinned (rule 16) |
| Services | all six active, 0 errors after the publish |

**Publish authority, for the record.** `configuration.publish` comes from **Department Lead** or
**Developer**, never Platform Admin — `ROLE_PLATFORM_ADMIN` holds only `user.manage`,
`role.manage`, `platform.manage`, `audit.view` (SEC-02 / ROLE-05 keep it out of legal
configuration). `ROLE_DEVELOPER` is every permission except `LEGAL_AUTHORITY_PERMISSIONS`, which
is why the Google/OIDC account could publish while the five Department User accounts could not.

### Release state — AB-20 code DEPLOYED, standards publish PENDING (2026-09-14, later)

Supersedes the section below, which recorded the state before the merge. That record stands as
written; this is what is true now.

**DEPLOYED.** PR #36 merged as **`a1b23e1d5277ea2f43b794a039d8e13cdd45d21e`**; the deploy tree
is on that commit, identical to `origin/main`, clean. Frontend rebuilt (`BUILD_ID`
`5XDhaMyKDnv2u0skbIdxv` → `9sjlCM6TCHGaX1i6S_u8v`), API and worker restarted. All six services
active with **0 restarts**, logs clean, TLS 1.3, smoke routes 200/302, database reachable over
SSL at head `e9f2b6c4a173`. Preflight on the deployed code: **17 PASS · 8 ATTEST · 0 FAIL**.

**The standards import and publish have NOT run.** Production holds **32 requirements, all
ACTIVE, 3 snapshots, 580 findings, 58 reviews** — unchanged. They are left as one atomic
operator step: publishing writes `actor_id` into the append-only audit trail, no API token is
stored, and locked **55.3** makes production credential creation a deliberate operator act
(`tools/dev_account.py` refuses on production for that reason). The commands and the validated
33-code payload are in [ops/production/README.md](../../ops/production/README.md).

**Off-server backups: BUILT AND TESTED, credential-gated.** `ops/production/backup.sh` now runs
two stages — local 14-day plaintext for fast recovery, off-server 90-day GPG AES-256 for
disaster recovery to the private CloudPe bucket `legalmind-production-backups` (`S3-INWEST2`),
via `ops/production/s3_object.py` on the already-installed apt `python3-boto3`. Verified on this
host 2026-09-14: encryption (AES-256, ciphertext confirmed not plaintext), byte-identical
decrypt, **restore into a scratch database matching production exactly** (32 / 580 / 58 / 3 /
1,578, 49 tables, alembic at head), local retention pruning, and 7 unit tests on the remote
retention guard. **Upload and download are the one untested stage** — no CloudPe credentials
exist on this host or in the repository.

**Also fixed:** `ops/deploy.sh` restarted the API but never `legalmind-worker`, which runs the
same code from the same tree and loads it once at startup — every deploy silently left a stale
worker. It now restarts the worker after the API is healthy.

**Unchanged and still outstanding:** the six AB-14 terminology failures remain **pre-existing,
non-regression** (identical on `main` before this deploy); encryption at rest and the egress
allow-list remain provider/owner items.

### Release state — AB-20, as of 2026-09-14

**PREPARED · REHEARSED · NOT DEPLOYED.** `feat/constitution-content-first` is pushed and
open as **PR #36**; CI reports `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`. The live
service still runs `main` at `68d4486` from `/root/Legalmind.v1`, with **32 requirements,
all ACTIVE**, and is unchanged by any of this session's work.

| Gate | State |
|---|---|
| Backend · frontend · browser suites | REUSED — 1,765 / 401 / 34, valid because no application file differs between the verified commit and the release head |
| CI on PR #36 | PASS — every job, including the corpus-expectation and visual-regression gates that failed first and were each resolved on their merits |
| Production preflight | 17 PASS · 8 ATTEST · 0 FAIL · 0 BLOCKED, against the live environment |
| Tier-2 assist gate (`AM-28`) | **SHIPPABLE** — all six quantities held; faithfulness and citation precision now measured at 1.0 rather than blocked |
| Migrations | AT HEAD `e9f2b6c4a173` — the deployment has **no** migration step |
| Backup / restore | VERIFIED 2026-09-14 by restoring a fresh dump into a scratch database (32 / 580 / 58 recovered exactly); the nightly cron path executed successfully rather than merely configured |
| Deployment rehearsal | COMPLETE against a scratch copy — see [ops/production/README.md](../../ops/production/README.md) for the two findings that would otherwise have broken it |
| `verify_terminology` (full run) | 34 PASS · **6 FAIL** — the six AB-14 reconciled standards, failing identically on `main`; **pre-existing, not a release regression**, and awaiting an owner ruling |
| Egress allow-list | **NOT SATISFIABLE as specified** — measured; needs a hostname-aware proxy, which needs approval under rule 19 |
| Encryption at rest | ATTESTATION OUTSTANDING — no OS-level encryption exists (`lsblk`: plain `ext4`); only the hosting provider can attest the volume |

Deploying additionally requires the standards imported **and** the 33 non-retired codes
published through `POST /configuration/publish` — the import alone leaves eight new
requirements `DRAFT` and therefore inert.

| Unit | Basis | State | Evidence |
|---|---|---|---|
| 1 · Database schema & migrations | Steps 41–42, AB-1, AB-2, Step 47, **AB-12** | IMPLEMENTED · TESTED | **30 application tables, 201 columns as of 2026-09-05** (AB-12 added `departments` and `users.department_id`, renamed `contracts.deleted_at` → `archived_at`; migration `b7c3d9e1f2a4`, exercised on a clone of the development database — the live database is NOT migrated). *Earlier:* **29 application tables** (+ `alembic_version` — the 30 counted by `AM-27` r2; see **C-14**); **196 columns** (195 until 2026-09-01, when `contracts.deleted_at` was added under `AM-37`/AB-10 — migration, lock record and snapshot moved in one change, which is the only procedure that may move this number); 21 invariant tests + **33 locked-column snapshot tests** (`test_locked_schema_columns.py`, new 2026-08-25 — `AM-27` r2 cites the invariant tests as its evidence that no locked table changed, and until this file existed none of them was sensitive to a column) | **2026-09-10 — Client Profiles (owner instruction):** migration `e9f2b6c4a173` widens `counterparties` from 7 to **19 columns** (total **209 → 221**) with the profile fields a client page shows — all nullable but `status` (NOT NULL, server default `ACTIVE`), and **nothing backfilled** (rule 21). **No table added** (application tables stay at **31**, so C-14 is untouched) and **no enum type added**: `status` and the three version roles are validated strings in `domain/client_profile.py`, the owner-Q2 `D-3` route. The version role (`COMPANY_DRAFT` / `CLIENT_MODIFIED` / `FINAL_SIGNED`) lives in locked 42.4's existing `metadata` JSONB — no column. `test_locked_schema_columns.py`'s snapshot moved in the same commit, which its docstring names as the only permitted way. ⚠️ **No `all_lock.md` record exists for this addition** — see the CHANGELOG's *Owner approval still owed*.
| 2 · Authentication & authorization | Step 47, **AB-12** | IMPLEMENTED · TESTED | **Platform Administration (2026-09-05)**: four sections (Users · Departments · Roles & permissions · Audit log) on the frozen AB-12 model — no locked decision amended; `GET /users` allow-listed filters + sort, one-act account creation (S-8 before the row exists), department rollups + rename, `GET /permissions`, audit actor/target resolution with the payload served for `admin.*`/`auth.*` only — **35 tests in `test_admin_platform.py`**, 11 Vitest, 7 browser. **AB-12 (2026-09-05): four personas, OWN/DEPARTMENT/PLATFORM scope, owner-only writes, transfer, archive, JIT whitelist — 28 persona-matrix tests (`test_rbac_personas.py`) + 17 archive tests (`test_contract_archive.py`) over the HTTP surface**, plus the earlier 24 authorization + 15 session tests, 21 API authz tests |
| 3 · Document storage & ingestion | Step 34 | IMPLEMENTED · TESTED | 33 ingestion tests (counted 2026-09-06; the cell read 20). **2026-09-06:** declared version metadata — source / counterparty / effective date — in locked 42.4's `metadata` JSONB via `PATCH /document-versions/{id}` (`document.upload`; frozen once a Review exists, owner ruling on 33.7 / 34.15 r3; no column, no migration); one COMPLETED processing run IS the document for every reader (`latest_completed_run_id` — pane, mapping, unmatched, comparison); 44.4 annexure titles marked where the document declares them (metadata-only, proven byte-identical segmentation across the corpus); `POST /document-versions/{id}/reprocess` — a NEW `REPROCESS` run (42.5) over the preserved original, refused (409) while a Review, an Ask citation or Key Obligations rely on the current reading (owner: Option C), audited; contract `status` (Step 2 Draft/Active/Superseded) declared via the existing PATCH and audited (`contract.status_changed`). **AB-13 (2026-09-06):** the counterparty is now an ENTITY — NEW `counterparties` table + `contracts.counterparty_id` (migration `c8e4a1b7d2f6`; **31 tables, 209 columns**), `GET /counterparties/{id}` returns the profile AND every contract for that company (relatedness DERIVED — no relationship table, and r3 forbids one); no new permission (`contract.view`/`contract.update`); visibility rooted in the contract or authorship, a global list forbidden (r6). **DEPLOYED 2026-09-06** — commits `2dcfac5`/`66a95d5`/DOCX-markers, migrations `b7c3d9e1f2a4` + `c8e4a1b7d2f6` applied to the live DB, API restarted, frontend BUILD_ID `c8uah2QHMuXQ7oOAUZm2G`, smoke-tested live |
| 4 · Mapping layer | Steps 28, 35 | IMPLEMENTED · TESTED | 24 mapping + 5 mapping-service tests |
| 5 · Evaluation engine | Steps 44, 45A–45D | IMPLEMENTED · TESTED | 64 evaluation + 28 corpus tests |
| 6 · Decision & review workflow | Steps 4, 22, 30, 31 | IMPLEMENTED · TESTED | 28 workflow tests |
| 7 · HTTP API | Steps 43, 47, 49 | IMPLEMENTED · TESTED | **45 operations** in the frozen contract ([docs/api/openapi.json](../api/openapi.json), drift-tested): the 39 locked by 49.3 plus the assist-lane conversation API (create · list · get-with-citation-replay · ask) and a read projection of Evidence, all recorded as implementation additions in Step 49's record section (2026-08-26); `GET /contracts/{id}` carries `document_versions` since 2026-08-30 (additive; the workspace's path to its document); **`DELETE /contracts/{id}` added 2026-09-01** under `AM-37`/AB-10 — two server-decided modes (hard when no Review exists, soft when one does), `contract.delete` scoped by `owner_id`, 16 dedicated tests; 140 API tests + the contract-shape suite · **2026-09-04: 56 operations**, the newest `GET /contracts/{contract_id}/version-comparison` — locked 33.15's deterministic clause comparison, declared `document.view` in `permission_map` (the route-coverage test refuses a route that declares none — it caught this one) with the OpenAPI snapshot regenerated in the same commit | **2026-09-10 — Client Profiles:** two NEW routes (`GET /counterparties/industries`, `GET /counterparties/{id}/activity`), pagination + `q`/`status`/`industry`/`has_documents`/`sort` + optional stats on `GET /counterparties`, `counterparty_id` (including the literal `none`) on `GET /contracts`, and `counterparty_id` on `POST /contracts`. **No new permission** — every route is `contract.view` or `contract.update` (AB-13 r5), and the activity feed deliberately is NOT `audit.view`. AB-13 r6's prohibition on a global counterparty list still holds; `test_no_endpoint_lists_every_counterparty` records the confrontation. Envelope unchanged: `data` is still the array every prior caller read.
| 8 · Frontend | Step 52 | IMPLEMENTED · TESTED | 10 of 10 locked 52.6 screens except export; **107** Vitest tests + **53** Playwright browser-workflow tests (57 runner items, including 4 setup steps), plus **18 gated specs** (6 visual-regression baselines behind `DESIGN_QA=1`, 3 documentation captures behind `DOCS_SHOTS=1`) that stay out of the default run so it remains order-independent. **2026-08-26: the core Review screen had crashed on load since 2026-08-24** (a hook declared below an early return — React #310), invisible to typecheck/build and to CI, which never ran on the working branch; fixed, the whole suite re-run green, and CI was widened to trigger on every branch push (narrowed back to `main` + pull requests on 2026-09-22 — decision 337; a PR is now mandatory for every change, and the duplicate run left every PR reading "Checks failing" with all fifteen jobs green). **2026-08-27 (Phase 4 hardening):** loading skeletons (layout-shape-matched, reduced-motion-safe), review-screen keyboard navigation (`n`/`p`/`d`/`?`; `a`/`r` prepare a decision and provably never POST one), and the 409 conflict flow now freezes the form until an explicit refresh (52.7) — full loop proven in the browser suite. **2026-08-30 (UI/UX slice 1, PRODUCT_UX_ROADMAP §G):** the new application begins at `/workspace/[contractId]` — its own shell, three-region layout collapsing to real tabs, and the document pane with the cross-pane highlight (outline click / `?evidence=` link → scroll + light + focus the exact evidence row), all states implemented and browser-proven; Findings and Ask regions are honest next-slice notes; legacy screens untouched (`LEGACY UI — DEFERRED`). **Same day, strict cleanup**: root `/` and login now land on `/workspace` (was `/contracts`); a new Documents index (`/workspace`) is the real landing screen; every link the new UI had into the legacy application was found and removed (`navItemsFor` now gates on the destination existing in the new UI, not just on permission — today that is Documents alone); the empty-workspace state gained a real inline upload instead of a link to the legacy contract page. Two real bugs fixed while proving this, not assumed: `Chrome` never rendered `{children}` in its signed-out branch, so `/`'s redirect-only page never mounted for a signed-out visit; and this Next.js version (16.3.1) does not complete a Server-Component `redirect()` through this app's provider tree, so `/` now uses the same `useRouter().replace()` pattern `login/page.tsx` already used successfully. Legacy routes are untouched and still directly reachable by URL — every legacy Playwright spec navigates that way already, never via a clicked link, so the verification harness is unaffected. **Same day, slice 2**: the Findings pane is live — decision queue first (52.5/DD-1), per-axis chips (classification/rule-outcome, filled only for a non-calm value — no severity ranking within an axis), evidence links wired to the slice-1 highlight mechanism, and `DecisionControl`/`EscalateControl` porting the legacy safety properties (no optimistic UI, 409-freeze-then-explicit-refresh) with the visual distinction the master prompt requires between an authority-exercising control and a decision-adjacent request. Resolves its Review via a `contract_id` filter the backend already allow-listed — zero backend change. **Slice 3, same day**: the Ask pane in the Inquiry register — colorless, three message types (answer / identical refusal / evaluator-routed pointer), citations wired to the highlight via a new additive `evidence_id` on every citation (the one backend change; frozen contract byte-identical). The ANSWERED path is static-render-pinned only: no generator credential exists until the `AM-31` gate opens, stated rather than faked. **Slice 4, same day**: the Documents landing/intake — type as the one required choice from Step 6's ten values (presentation copy, backend parity test), create → open the workspace, first-run/skeleton/error/permission states. **Slice 5 (P1), 31 Aug**: the Reviews queue (REC-09 scope server-side, allow-list status filters), the Review report (counts only — F-9 stated on the page), and Ask history (own conversations, transcripts replaying the live citations and the byte-identical refusal). **Slice 6, same day**: the Legal area — the cross-review DECISION_REQUIRED queue (composed from existing endpoints), deep-linking `?finding=` into the workspace focused on the exact card. **Slices 7–8 + QA close, same day**: the admin plane (users/roles with the 47.1.3 no-roles birth state and SEC-05 refusals rendered verbatim; the append-only audit trail with 49.6 exact filters) and the Research disclosed placeholder (C-16) — the §G build order (1–10) is COMPLETE; the new shell carries Documents · Reviews · Legal · Ask history · Research · Admin. **UI FROZEN 2026-08-31** (decision #222) after an owner-ordered UX audit (6 fixes) and final visual QA: **15 CI-cut visual baselines** (6 legacy-era + 9 new-UI), CI 15/15 green at f9c3c0f. **2026-08-31 (owner UX rethink — the explicit UX-review request the freeze anticipated; decisions #231–#241):** the findings pane became the summary→category→finding→evidence drill (clickable classification counts; verbatim evidence excerpts beside findings; the rule-12 explanation chain restored to the workspace card); the all-MATCH success state designed; report and Documents count chips deep-link `?classification=`; finding→Ask handoff (editable draft, never auto-sent); the Ask pane restores the contract's latest conversation; a revised version chains analysis exactly like a first upload; Documents gained the "Needs attention" group; review-in-flight polls the lifecycle honestly; upload preflight messages; **PDF/DOCX export** buttons (see unit 5's export row). Now **113** Vitest + browser **57 passed / 18 gated**. Expected: new-UI visual baselines re-cut from CI. **2026-09-02 (owner report — Ask; DD-15, decisions #278–#283):** Ask stopped being a layout row. It is a 44px launcher plus a NON-modal panel (`role="dialog"`, `aria-modal="false"`, Escape closes, focus moves in and back out but is never trapped), absolutely positioned inside `.ws-workmain` so the three panels take the whole workspace height — the bar had reserved ~128px unconditionally. Deliberately NOT hover-to-open (unusable on touch; opens by accident while reading). Below 620px it is a bottom sheet with a dismiss scrim. Ask is **no longer disabled on an older version**: the workspace passes the open version to the ask endpoint, so it answers about the document on screen and names which version in its header; a turn answered from another version is labelled and its citations offer to open that version rather than a highlight that cannot land. `AskBar` → `AskDock`; the browser suite found two defects a static test cannot see (`display: inline-flex` out-specifying the UA `[hidden]` rule so the launcher stayed over the open panel — WCAG 2.2 AA 2.4.11; and focus-restore running before React unhid it). One journey assertion was **inverted**, deliberately: it had pinned the disabled-input behaviour the owner reported. Now **184** Vitest + browser **71 passed / 18 gated** · **2026-09-04:** the version-comparison panel (33.15/33.16 — clause-level, no verdict field and deliberately no status colour, unchanged clauses collapsed rather than dropped) and the workspace keyboard-shortcut layer. **187 Vitest + 77 Playwright** (14 gated), measured 2026-09-04 | **2026-09-10 — Client Profiles** (`/dashboard/clients`, nav item gated on `contract.view` alone, deliberately not admin-only): a client directory and a client workspace — a 236px rail plus one **unified** legal-documents list in which document type is a column and a filter and **never a grouping**, with version history as a disclosure under each document. Reuses the existing workspace, `chainAnalysis`, download endpoint and version comparison (`?compare=1` wired into `WorkspacePage`); **no framework, component library or dependency added** (rule 19), one plain-CSS `.ws-cl*` block. **+29 Vitest**, **+12 Playwright** (`e2e/client-profiles.spec.ts`). The rendered UI was reviewed in a browser at 1440/1366/1024 and nine defects were fixed there — see the CHANGELOG.
| 9 · Analysis orchestrator | Steps 28, 34, 35, 44 | IMPLEMENTED · TESTED | 20 orchestrator + 19 extraction tests |
| 10 · Golden corpus harness | Steps 45E, 54 | PARTIAL | Runner complete; **67 fixture entries — 21 `STRUCTURAL` + 9 `DOCUMENT_SUPPORTED` + 37 `STANDARD_DERIVED`**, counted from `backend/tests/corpus/*.json` on 2026-09-02 (this cell read "32 fixtures — 16 + 9 + 7" and CLAUDE.md read "28 authored — 16 + 9 + 3"; both were stale and neither matched the repository — count before quoting). Company Standards are **per document type** (owner Q3=B, 2026-08-19): `LIABILITY-MSA-001` (6 months, affected-service fees, `MSA.pdf` §17.2) and `LIABILITY-TOS-001` (12 months, total fees, `TOS-leapswitch.pdf` §13 — the 2026-08-18 value, re-scoped unchanged), each referenced by `company_standard_ref`. **The Legal Rule EXISTS and is wired** — the zero-tolerance rule (manager ruling 2026-08-19, owner approval to wire 2026-08-20) is present in **32 of 32** files under `backend/config/company_standards/` as `legal_rule.configuration = {"deviation_outcome": "UNACCEPTABLE", "unlimited_outcome": "UNACCEPTABLE"}`; both evaluators read it (`evaluation/numeric.py`, `evaluation/presence.py`) and `tools/import_ratified_standards.py` refuses any other value. **This cell previously read "No Legal Rule exists", which was stale and has been corrected against the repository (2026-08-25).** A Rule Outcome is `NOT_APPLICABLE` only where no rule applies, and un-ruled deviations still route to a human. Coverage of all 64: `backend/tests/corpus_coverage.json` — **17 AUTHORED · 1 AUTHORED_RATIFIED · 3 PARTIAL · 12 BLOCKED · 6 UNSTARTED · 4 OUT_OF_V1_SCOPE · 13 STRUCTURAL_ONLY · 8 SEPARATE_TRACK**. Still owed by the owner: a second tranche (11), one scope reading (1) — the Legal Rule is no longer outstanding. **No `NORMATIVE` fixture exists** |
| 11 · Observability & deployment | Steps 53, 55 | IMPLEMENTED · TESTED | **Step 53 complete** — 53.1–53.5 implemented, all seven named signals now have emission sites (36 tests). **Step 55 complete as specifiable** — **24-check** preflight register covering every 55.2 row (18 → 22 as the assist-lane checks landed; 23 with `egress_allow_list`, `AM-30` t8, added 2026-08-27; 24 with `jwt_signing_key`, `AM-36` t5, added 2026-09-01 — count verified by running it), the 55.4 r3 / 55.5 post-migration reproducibility gate (`tools/verify_reproducibility`), queue-backed worker, Dockerfiles, compose — now network-segmented: `data` internal (no route out) vs `edge`, with model weights mounted read-only (25 tests). CI: **15 jobs** (see unit 12 — job 14, A10, added 2026-08-26; job 15, Design QA, added 2026-08-27), job 1 blocking at a zero baseline. **Retention policy, log aggregation, alert thresholds and the monitoring stack remain NOT YET SPECIFIED** and are reported as `BLOCKED`/`ATTEST` rather than assumed. The `oidc` check changed shape on 2026-09-01: the flow is implemented, so it is now a pure configuration check — `PASS` once the deployment supplies issuer/client id/secret/redirect URI, `FAIL` on a plaintext or wrong-route redirect URI, `BLOCKED` while unset. `jwt_signing_key` reports `AM-36` t5: `FAIL` on a key under 32 bytes, `PASS` when absent (no token is issued and the revocable session carries the sign-in — the safer posture) and `PASS` with the revocation caveat stated when set |
| 12 · Assist lane (AI/RAG) | AB-3 (`AM-25`–`AM-29`), AB-4 (`AM-30`, `AM-31`), `IMPL-02`, **AB-14 (`AM-43`–`AM-48`)** | **IN PROGRESS** — Gate §5b **A0–A7 complete**; **A8 built 2026-09-08** (Domains A and C wired; statute corpus 17/18 — Income-tax Act 2025 outstanding); **A9 measurable-half done**; **A10 partial** | **A3 complete**: embedding model **selected by measurement** on the owner-ratified 77-question set — `all-MiniLM-L6-v2` (384d, 23M, smallest-that-passes per `AM-26` r2; hit@10 0.938) — and the refusal gate **calibrated from the measured distributions** (cosine ≥ 0.50 AND top-gap ≥ 0.059, OR a lexical hit: 12/13 unanswerable refused, J 0.564; full evidence in [BACKEND_ARCHITECTURE.md](../05-architecture/BACKEND_ARCHITECTURE.md)). `chunk_embeddings` created (migration `c4a91f6e2d87`, dimension a DDL literal); embeddings written at index time with graceful lexical-only degradation; gated hybrid retrieval in SQL with authorization inside the query. **A4 complete**: `guardrails.py` — mechanical citation verification (existence + grounding) and the three `AM-29` refusal outcomes; imports no prompt or model code (`AM-28` r2, asserted). **A5/A6 complete at application level**: Gemini adapter over stdlib urllib (**no SDK — rule 19 never triggered**), the **only** `EGRESS_ALLOWED` module; `AM-31` gate **CLOSED** — production egress refused in code, dev/staging (synthetic-only per 55.3) work with `LEGALMIND_GEMINI_API_KEY`; every call audited with a payload **hash** (`AM-30` t5). **Owner reaffirmed 2026-08-26: the gate stays CLOSED until Google's written no-training terms are confirmed** — no code change, the gate's own default. **A7 complete**: conversation API (3 endpoints behind new `assist.ask`), workspace Ask panel with citations, quiet refusals, and retrieval scores labelled as retrieval scores — no confidence figure anywhere. **A10 partial, 2026-08-26**: CI job 14 runs pip-audit and npm audit (both measured zero findings, blocking) and Trivy against both built Dockerfiles (CRITICAL/HIGH blocking, MEDIUM/LOW reported) — the first CI build test either image has had, and its first real scans caught 3 HIGH in the backend base image (one openssl CVE, fix already published — resolved by distro upgrades at build time) and 9 CRITICAL/HIGH in the frontend image's *bundled npm* (not our dependencies; resolved by removing every package manager from the runtime image and starting Next via `node` directly); and `docker-compose.yml` is network-segmented (`data` internal — no route out — for db/queue/workers/api; `edge` for frontend/api; weights by read-only mount since the data network cannot download). OpenVAS/ZAP deliberately deferred to the deployment pipeline (`AUTO_MODE_DECISIONS.md` #143) — they scan a running instance, which CI does not stand up. **A9 measurable-half done, 2026-08-26**: `tools/verify_assist_quality.py` is `AM-28`'s gate as a runnable release-pipeline command — every ratified question through the production `search_hybrid`, blocking on a worsened wrongly-answered rate vs `tests/assist_eval/baseline.json` (live-recorded: 12/13 refused, 41/64 retained, end-to-end recall@10 0.438 — decomposed: 12 of 13 gate-open misses are the deliberate sub-floor evidence rule); proven to fail (exit 1 on a tightened baseline) and named by the new `tier2_quality_gate` preflight check. Faithfulness/citation-precision stay blocked on `AM-31` (m4 forbids a synthetic substitute). Backend **894 tests** (1 skipped), frontend **62 Vitest + 22 Playwright**. Network-level egress allow-list (t8, full destination enumeration) and the `legalmind_assist` DB role remain deployment items. **AB-7 added 2026-08-31**: `generate_raw()` — the second/third prompts through the SAME egress seam (import-boundary test unchanged as proof); type suggestion (AM-34, `assist.ask`, audit `assist.type_suggestion_called`); Key Obligations (AM-35, migration `e5b8d3f17a2c`, mechanical judgment-language screen, grounded-or-discarded, `finding.view`). **2026-09-02 — the ask target is explicit (decisions #278–#283, DD-15):** `POST /conversations/{id}/messages` had resolved `MAX(version_number)` over the conversation's contract with no way to say otherwise, so a reader on version 1 was answered from version 2 and every citation's `evidence_id` belonged to a reading order absent from the open page. `AskRequest` now takes an **optional** `document_version_id` (omitted still means the newest, so the change is additive — `docs/api/openapi.json` regenerated, +13 lines); it is authorized through the full `guard.document_version` chain **and** required to equal the conversation's `contract_id`, so it can only narrow scope; a malformed id is refused, never silently widened to the newest. The document scope is recorded in `retrieval_runs.filters` — the column `AM-27` names for it — with **no schema change**, and `GET /conversations/{id}` reports each turn's version (falling back to the run's first chunk for rows written earlier). **Nothing locked was amended:** `AM-25`/`AM-27` fix nothing about conversation-to-version scope and `conversations` carries `contract_id` only, so the existing contract-scoped model was made explicit rather than replaced. 9 new backend tests. **2026-09-08 — AB-14 (`AM-43`–`AM-47`): the router, Domain A wired, Domain C built.** Measured live as a Department User over the HTTP API: the manager's comparison question and three descriptive questions the document answered were all refused. Root causes and fixes — (1) **chunker `clause-aware-3`**: 29 % of the index was sub-60-char headings that outranked their clauses (`7. TERM AND TERMINATION`), and U+200B after a clause number made §17 of a real MSA one 1,366-char chunk; headings now fold into the clause they introduce, U+200B/NBSP are blanks; **test-account contracts reindexed only** — real users' contracts await an operator reindex after deploy. (2) **`assist.intent.is_comparison_question`** replaces the `AM-25` r4 regex that missed "comply" and "our approved position"; 30-phrase matrix. (3) **`assist.routing.plan`** — candidate domains from the caller's permission set first, question shape second, recorded on `retrieval_runs.filters.domains`; no source selector anywhere. (4) **Domain A wired** (`search_positions` had zero callers): verbatim quotes with standard code · clause · type in their own field, never in a generation payload (asserted); OR-lexeme search with a two-lexeme floor; permission `assist.ask` AND (`configuration.view` OR `legal_position.view`) — `AM-44`. (5) **Domain C built** (`assist/statutes.py`, `tools/ingest_statutes.py`, `config/statutes/registry.json`): section-based chunks (`section-1`, footnote-prefixed numbering and the arrangement table handled), provenance-refusing ingestion, Act + section citations, its own generation call over statute evidence only, `answer_citations.statute_chunk_id`; **the seven supplied statutes ingested into the dev DB**; NI Act / Evidence Act absent (C-16) — the refusal names the corpus's holdings. (6) **Refusals** one wording per candidate set (`AM-46`); document-less conversations are legitimate (`/dashboard/research` is now a real Ask surface). (7) **Intake** no longer gates on document type; analysis still refuses an undeclared type. (8) **Six standards reconciled to the Constitution** (`AM-43` r4); corpus 73 fixtures (21/9/43). **Later on 2026-09-08 (`AM-48`):** the statute corpus was completed from India Code / MeitY / DoR — **17 instruments, 5,140 sections** (NI Act, Arbitration Act, CPC, Copyright Act, BSA 2023, CGST/IGST, Companies Acts 2013 and 1956, DPDP Rules 2025 added; the three owner-supplied central Acts replaced by India Code as-on texts); C-16 resolved; **Income-tax Act 2025 not obtainable** from an official host. Section-start regex accepts `73.Title` (no space); the named Act ranks first among same-numbered sections. Not done: real-user contract reindex (operator), Entity/Brand axis (C-19), the Constitution positions with no standard yet (`AM-43` r6), statute vectors are best-effort (lexical-first). **DEPLOYED 2026-09-08 22:53 IST** (owner "ok deploy"; HEAD `22d3372`; API restarted, frontend build `afnrSH0wgHtiloVDSjI1l`; no migration). Pending operator steps blocked by session permissions: live import/publish of the six reconciled standards (live config still pins the 2026-09-01 values), reindex of 40 `clause-aware-2` versions. Frontend: the Finding card's user-facing status is Accepted / Needs review / Not accepted via `findingLanguage.userStatus()`; the four classifications unchanged in data and disclosure; `Not accepted` keyed to `Evaluation.constitution_prohibition`, **not yet emitted by the API**. **2026-09-09 — AB-15 (`AM-49`) BUILT:** the grounded explanation layer — one validated plain-English sentence per Finding from the approved description + classification phrase + cited passages, cached by source hash in `finding_explanations`, `finding.view`, hash-only audit; 23 backend tests (payload contents, injection, unsupported claims, cache, invalidation, permissions, byte-identical Finding rows) + 3 frontend; NOT deployed. **`AM-28` Tier-2 gate re-run after `AM-48` (2026-09-08): SHIPPABLE** — wrongly answered 1/13 (baseline ≤ 1), correct refusals 12/13, retained 43/64 (≥ 41), recall@10 0.469 (≥ 0.438), hit@1 0.344, faithfulness 1.0, citation precision 1.0, user-visible wrong 0/13. **2026-09-10 — the document is context, not the knowledge boundary:** `RoutePlan.fallback` + one convergence point consulting every authorized source before any refusal (gate closed, thin evidence, model NOT FOUND, failed verification, unavailable model all included); priority document → position → statutes (statutes for a law question or a silent position; semantic evidence required for a non-law question); `is_verdict_statement` replaces the router's stems on generated text; retrieval `hybrid-rrf-gate-2` (calibrated AND gate unchanged, OR-floor candidates, fragment prune); Domain A `position_chunk_embeddings` filled (32) and hybrid; Domain C rank-fused when nothing is named. **Tier-2 gate SHIPPABLE:** wrongly answered 1/13, recall@10 **0.625**, retained 43, faithfulness 1.0, citation precision 1.0; the OR-as-gate variant was measured at 13/13 and rejected. Live-verified in-process against the production DB (owner's question answered from the document with four citations). 24 new tests; NOT deployed. **2026-09-17 — Ask target architecture, Phase 0 DEPLOYED (`663971f`):** prose citation markers and the citation list carry the same number (renumbered after verification, persisted in display order); `AM-67` answers record `position-reading-aid-1`; every ask timed stage by stage (`assist.ask.timings`); the Tier-2 gate re-verifies against the citations the markers index (its first run read faithfulness 0.758 through a mis-indexed list — proven and corrected), and reports per-stage p50/p95 and Gemini calls/tokens per question. Shipped gate: wrongly-answered 1/13, user-visible wrong 0/13, recall@10 0.891, faithfulness 1.0. **Phase 1 BUILT, MEASURED, NOT MERGED (PR #69, owner decision):** the query planner — accurate plans, 2.4–8.0 s each from the provider; planner off→on on the ratified 77: MRR 0.701→0.728, gold@3 0.797→0.828, hit@1 0.594→0.609 (each ≈ one question, inside the rescue's run-to-run swing), evidence precision 0.390→0.358, total p50 5.9→10.6 s, Gemini calls/q 1.23→2.25, safety held. Ships OFF; by design a reformulation cannot open the calibrated gate. The evidence points at the reranker (Phase 2, already authorised by `AM-25`/`AM-26`). `LEGALMIND_POSITION_SYNTHESIS` (`AM-67`) enable pending the owner's word; r7 verified on the live corpus (0 paths, 0 counterparty names). **2026-09-17 — Ask Phase 2 DEPLOYED and ENABLED (`d507f2c`, PR #74; code restart 13:07:34 IST, flag restart 13:08:31 IST):** a local cross-encoder REORDERS retrieved evidence — `cross-encoder/ms-marco-MiniLM-L-6-v2@233902d2`, pinned, SHA-256 verified, CPU-only, selected by `AM-26` r2 bakeoff (TinyBERT-L-2 FAILED — worse than no reranker; L-12 no better at 2× latency). **No amendment needed** — `AM-25`'s permitted list and `AM-26`'s stack table already name a local self-hosted open-weight cross-encoder. Reorder only: the calibrated gate keeps its inputs and decision, evidence membership is fixed inside `search_hybrid` before the reranker is reached, citation verification untouched, score never rendered. Measured off→on: hit@1 0.609→0.734 (four runs), MRR 0.709→0.796, gold@3 0.797→0.844; wrongly-answered 1/13, user-visible wrong 0/13, false refusals 3, faithfulness and citation precision 1.0 ALL unchanged; rerank stage 126/469 ms, live steady-state 133 ms. Verified in production: `assist.rerank.loaded` in the journal, three live Asks through it, 0 errors, NRestarts 0. **A rerank floor CANNOT reopen a shut gate** — false-refusal top score median −3.12 against unanswerable −2.30 (higher), overlapping; fourth feature to fail that separation. Baseline re-recorded under the shipped pipeline. **Phase 1 query planner PARKED** (measured: no demonstrated targeting gain for +4.6 s p50) — `planner.py` is absent from the deployed tree.. **Domain C corpus repair — IMPLEMENTED AND VALIDATED IN SCRATCH, NOT DEPLOYED (2026-09-21).** `section-3`: a Schedule is its own citable unit, and a footnote marker glued to a section number (`²66A.` -> `266A.`) no longer swallows the rest of the Act — six such blobs held 1,685 chunks / 57% of Domain C text and occupied 31% of every returned slot. Bounded by the Act's own arrangement of sections, refusing itself past five rewrites (the Taxmann Income-tax print, left untouched); three numbers rewritten corpus-wide; text-neutral across all 21 PDFs. Ranking: a FRACTIONAL `act_match` no longer sorts first and a REPEALED Act sorts last among equals. Measured on a scratch re-ingest at production `limit=6`, zero Gemini (`tools/probe_statutes.py`): recall@6 0.700 -> **0.800**, hit@3 0.500 -> **0.650**, citation correctness 13/14 -> **16/16**, hit@1 0.400 -> 0.400, named-Act and exact-section unchanged, latency flat. **The deployed corpus still carries `section-1` markers** — it predates even the 2026-09-20 reading-order fix — so DEPLOYING THIS REQUIRES A PRODUCTION STATUTE RE-INGEST, which has not been run.. **Domain C knowledge-domain ROUTING rebuilt and FROZEN at `1a7ce69` (2026-09-21).** `is_statute_question` was one flat regex matched anywhere — it fired on the English VERB "act" and recognised only 2 of the 23 statute questions, so the rest were held to `require_semantic`. It is now a relation over seven deterministic signals (names_instrument · jurisdiction · rule_framing · legal_actor, against document_target · position_target · first_person), in the idiom `is_comparison_question` was rebuilt into on 2026-09-16; `RoutePlan.statute_signals` records which fired. Measured on a 91-case labelled matrix: GENERAL_LAW precision **1.000** held, recall **0.333 -> 0.889**, false STATUTE **0**; Domain C evidence reaches **20/20** answerable (was 15) and the contract lane is unmoved at 9/54. **`require_semantic` now blocks 0 questions (was 8) and `COSINE_FLOOR` is unchanged at 0.50** — routing decides whether the gate applies; the gate itself was not touched. Suite 2249 passed / 112 skipped / 198 s, lint and mypy clean. ⛔ **GEMINI END-TO-END VALIDATION IS PENDING AND IS THE ONE EXTERNAL BLOCKER** — the provider returns HTTP 402 (credit exhausted). The last confirmed end-to-end figures remain the PRE-ROUTING baseline (13/20 answered, 13/13 cited correctly, 0 leakage); the post-routing answer quality is NOT established and is not claimed here. Nothing pushed beyond `c8af919`, nothing merged, deployed or re-indexed. **2026-09-23 — `AM-77` (AB-27), the final Ask readiness P0, BUILT and measured:** a Company Standard is no fallback for a question about the law, and a fall-through position qualifies only through the calibrated vector gate (Domain C's `require_semantic`, mirrored); "is this MSA ok for us?" routes to the evaluator. 76-case matrix: law questions answered with a position 12 → 0, must-refuse 6/6, Gemini/q 1.87 → 1.24; readiness-set unrelated-position answers 4 → 0. Suite 2522 passed / 112 skipped / 0 failed. **`AM-77` DEPLOYED 2026-09-23 (`3114113`).** **2026-09-23 — `AM-78` (AB-28):** the reader's unevidenced figure is named by a fixed deterministic sentence (`guardrails.unstated_figures`), prompt `position-reading-aid-5` answers the reader's situation first, a fixed Legal-review line follows every verified position explanation, and the position explanation (`LEGALMIND_POSITION_SYNTHESIS`, `AM-76` r1's default) is enabled in production with this release. Verifier unchanged. |
| 12 · Assist lane (AI/RAG) — main's 2026-09-09 deploy record, retained on merge | AB-3 (`AM-25`–`AM-29`), AB-4 (`AM-30`, `AM-31`), `IMPL-02`, **AB-14 (`AM-43`–`AM-48`)** | **IN PROGRESS** — Gate §5b **A0–A7 complete**; **A8 built 2026-09-08** (Domains A and C wired; statute corpus 17/18 — Income-tax Act 2025 outstanding); **A9 measurable-half done**; **A10 partial** | **A3 complete**: embedding model **selected by measurement** on the owner-ratified 77-question set — `all-MiniLM-L6-v2` (384d, 23M, smallest-that-passes per `AM-26` r2; hit@10 0.938) — and the refusal gate **calibrated from the measured distributions** (cosine ≥ 0.50 AND top-gap ≥ 0.059, OR a lexical hit: 12/13 unanswerable refused, J 0.564; full evidence in [BACKEND_ARCHITECTURE.md](../05-architecture/BACKEND_ARCHITECTURE.md)). `chunk_embeddings` created (migration `c4a91f6e2d87`, dimension a DDL literal); embeddings written at index time with graceful lexical-only degradation; gated hybrid retrieval in SQL with authorization inside the query. **A4 complete**: `guardrails.py` — mechanical citation verification (existence + grounding) and the three `AM-29` refusal outcomes; imports no prompt or model code (`AM-28` r2, asserted). **A5/A6 complete at application level**: Gemini adapter over stdlib urllib (**no SDK — rule 19 never triggered**), the **only** `EGRESS_ALLOWED` module; `AM-31` gate **CLOSED** — production egress refused in code, dev/staging (synthetic-only per 55.3) work with `LEGALMIND_GEMINI_API_KEY`; every call audited with a payload **hash** (`AM-30` t5). **Owner reaffirmed 2026-08-26: the gate stays CLOSED until Google's written no-training terms are confirmed** — no code change, the gate's own default. **A7 complete**: conversation API (3 endpoints behind new `assist.ask`), workspace Ask panel with citations, quiet refusals, and retrieval scores labelled as retrieval scores — no confidence figure anywhere. **A10 partial, 2026-08-26**: CI job 14 runs pip-audit and npm audit (both measured zero findings, blocking) and Trivy against both built Dockerfiles (CRITICAL/HIGH blocking, MEDIUM/LOW reported) — the first CI build test either image has had, and its first real scans caught 3 HIGH in the backend base image (one openssl CVE, fix already published — resolved by distro upgrades at build time) and 9 CRITICAL/HIGH in the frontend image's *bundled npm* (not our dependencies; resolved by removing every package manager from the runtime image and starting Next via `node` directly); and `docker-compose.yml` is network-segmented (`data` internal — no route out — for db/queue/workers/api; `edge` for frontend/api; weights by read-only mount since the data network cannot download). OpenVAS/ZAP deliberately deferred to the deployment pipeline (`AUTO_MODE_DECISIONS.md` #143) — they scan a running instance, which CI does not stand up. **A9 measurable-half done, 2026-08-26**: `tools/verify_assist_quality.py` is `AM-28`'s gate as a runnable release-pipeline command — every ratified question through the production `search_hybrid`, blocking on a worsened wrongly-answered rate vs `tests/assist_eval/baseline.json` (live-recorded: 12/13 refused, 41/64 retained, end-to-end recall@10 0.438 — decomposed: 12 of 13 gate-open misses are the deliberate sub-floor evidence rule); proven to fail (exit 1 on a tightened baseline) and named by the new `tier2_quality_gate` preflight check. Faithfulness/citation-precision stay blocked on `AM-31` (m4 forbids a synthetic substitute). Backend **1,569 tests collected** (counted 2026-09-09 with `pytest --collect-only`; the figure here read "894 tests (1 skipped)" and was stale — count before quoting, per the greeting protocol), frontend **339 Vitest across 25 files + 27 Playwright spec files**. Network-level egress allow-list (t8, full destination enumeration) and the `legalmind_assist` DB role remain deployment items. **AB-7 added 2026-08-31**: `generate_raw()` — the second/third prompts through the SAME egress seam (import-boundary test unchanged as proof); type suggestion (AM-34, `assist.ask`, audit `assist.type_suggestion_called`); Key Obligations (AM-35, migration `e5b8d3f17a2c`, mechanical judgment-language screen, grounded-or-discarded, `finding.view`). **2026-09-02 — the ask target is explicit (decisions #278–#283, DD-15):** `POST /conversations/{id}/messages` had resolved `MAX(version_number)` over the conversation's contract with no way to say otherwise, so a reader on version 1 was answered from version 2 and every citation's `evidence_id` belonged to a reading order absent from the open page. `AskRequest` now takes an **optional** `document_version_id` (omitted still means the newest, so the change is additive — `docs/api/openapi.json` regenerated, +13 lines); it is authorized through the full `guard.document_version` chain **and** required to equal the conversation's `contract_id`, so it can only narrow scope; a malformed id is refused, never silently widened to the newest. The document scope is recorded in `retrieval_runs.filters` — the column `AM-27` names for it — with **no schema change**, and `GET /conversations/{id}` reports each turn's version (falling back to the run's first chunk for rows written earlier). **Nothing locked was amended:** `AM-25`/`AM-27` fix nothing about conversation-to-version scope and `conversations` carries `contract_id` only, so the existing contract-scoped model was made explicit rather than replaced. 9 new backend tests. **2026-09-08 — AB-14 (`AM-43`–`AM-47`): the router, Domain A wired, Domain C built.** Measured live as a Department User over the HTTP API: the manager's comparison question and three descriptive questions the document answered were all refused. Root causes and fixes — (1) **chunker `clause-aware-3`**: 29 % of the index was sub-60-char headings that outranked their clauses (`7. TERM AND TERMINATION`), and U+200B after a clause number made §17 of a real MSA one 1,366-char chunk; headings now fold into the clause they introduce, U+200B/NBSP are blanks; **test-account contracts reindexed only** — real users' contracts await an operator reindex after deploy. (2) **`assist.intent.is_comparison_question`** replaces the `AM-25` r4 regex that missed "comply" and "our approved position"; 30-phrase matrix. (3) **`assist.routing.plan`** — candidate domains from the caller's permission set first, question shape second, recorded on `retrieval_runs.filters.domains`; no source selector anywhere. (4) **Domain A wired** (`search_positions` had zero callers): verbatim quotes with standard code · clause · type in their own field, never in a generation payload (asserted); OR-lexeme search with a two-lexeme floor; permission `assist.ask` AND (`configuration.view` OR `legal_position.view`) — `AM-44`. (5) **Domain C built** (`assist/statutes.py`, `tools/ingest_statutes.py`, `config/statutes/registry.json`): section-based chunks (`section-1`, footnote-prefixed numbering and the arrangement table handled), provenance-refusing ingestion, Act + section citations, its own generation call over statute evidence only, `answer_citations.statute_chunk_id`; **the seven supplied statutes ingested into the dev DB**; NI Act / Evidence Act absent (C-16) — the refusal names the corpus's holdings. (6) **Refusals** one wording per candidate set (`AM-46`); document-less conversations are legitimate (`/dashboard/research` is now a real Ask surface). (7) **Intake** no longer gates on document type; analysis still refuses an undeclared type. (8) **Six standards reconciled to the Constitution** (`AM-43` r4); corpus 73 fixtures (21/9/43). **Later on 2026-09-08 (`AM-48`):** the statute corpus was completed from India Code / MeitY / DoR — **17 instruments, 5,140 sections** (NI Act, Arbitration Act, CPC, Copyright Act, BSA 2023, CGST/IGST, Companies Acts 2013 and 1956, DPDP Rules 2025 added; the three owner-supplied central Acts replaced by India Code as-on texts); C-16 resolved; **Income-tax Act 2025 not obtainable** from an official host. Section-start regex accepts `73.Title` (no space); the named Act ranks first among same-numbered sections. Not done: real-user contract reindex (operator), Entity/Brand axis (C-19), the Constitution positions with no standard yet (`AM-43` r6), statute vectors are best-effort (lexical-first). **DEPLOYED 2026-09-08 22:53 IST** (owner "ok deploy"; HEAD `22d3372`; API restarted, frontend build `afnrSH0wgHtiloVDSjI1l`; no migration). Pending operator steps blocked by session permissions: live import/publish of the six reconciled standards (live config still pins the 2026-09-01 values), reindex of 40 `clause-aware-2` versions. Frontend: the Finding card's user-facing status is Accepted / Needs review / Not accepted via `findingLanguage.userStatus()`; the four classifications unchanged in data and disclosure; `Not accepted` keyed to `Evaluation.constitution_prohibition`, **not yet emitted by the API**. **2026-09-09 — AB-15 (`AM-49`) BUILT:** the grounded explanation layer — one validated plain-English sentence per Finding from the approved description + classification phrase + cited passages, cached by source hash in `finding_explanations`, `finding.view`, hash-only audit; 23 backend tests (payload contents, injection, unsupported claims, cache, invalidation, permissions, byte-identical Finding rows) + 3 frontend; NOT deployed. **`AM-28` Tier-2 gate re-run after `AM-48` (2026-09-08): SHIPPABLE** — wrongly answered 1/13 (baseline ≤ 1), correct refusals 12/13, retained 43/64 (≥ 41), recall@10 0.469 (≥ 0.438), hit@1 0.344, faithfulness 1.0, citation precision 1.0, user-visible wrong 0/13. |

Backend: **1,488 passed**, 142 skipped, 1 xfailed. Frontend: **380** Vitest tests in 28 files, typecheck and `check:terms` clean. Both measured 2026-09-10 **after rebasing Client Profiles onto the workspace fix pass** (PR #23), whose own baseline is **1,454 backend / 351 frontend** — so Client Profiles contributes **+34 backend and +29 frontend** tests and changes no skip (142 is PR #23's own skip count, all embedding-model and statute-corpus gates). An earlier reading of 1,455 → 1,488 / 339 → 368 was taken before PR #23 existed and is superseded here rather than left to look like a regression. Previous measurement, retained for the reasoning it carries: **1,252 tests** (measured 2026-09-05 after the administration work — **1,249 passed**, 1 skipped, 1 xfailed, and **1 failing by design until deployment**: `test_migrations_must_be_at_head` reads the live development database, which is deliberately not migrated to `b7c3d9e1f2a4`; it passes against a migrated clone. Frontend **206 Vitest**, browser **86 passed / 14 skipped / 0 failed**.) *Earlier:* **1081 tests**, 1 skipped, all green as of 2026-09-01 (AB-10: +16 contract-deletion tests; OpenAPI snapshot regenerated to **55 operations**. Earlier: AB-7's +11 type-suggestion, +8 obligations, +permission-map/OpenAPI updates) (AM-33 regressions + the 2026-08-31 UX-correction reads: snapshots list, layered list summary) — the skip is `test_assist_schema.py`'s `legalmind_assist` grant check, which asserts its property only where the role exists; the role is a deployment precondition and `preflight` reports its absence as BLOCKED — the six supplied documents are present at `LEGALMIND_SOURCE_MATERIAL_DIR` and parse `COMPLETE`. Frontend: **117 Vitest + 60 Playwright** (57 runner items with setup; 18 further specs gated — 15 visual baselines, 3 documentation captures). All green. `ruff` and `mypy` are at **zero** and CI job 1 is blocking. CI is **15 jobs** (job 14 added 2026-08-26 — dependency and container scanning, A10; job 15 added 2026-08-27 — Design QA: the rule-12 forbidden-terms gate and 0.1%-threshold visual regression) and triggers on **pull requests and pushes to `main`** (decision 337, 2026-09-22). It ran on every branch push from 2026-08-26 to 2026-09-22 — that widening followed five days of feature-branch commits with no job run at all, and was reversed once AGENTS.md §1 made a pull request mandatory, because `push` + `pull_request` produced two runs per commit and the `concurrency`-cancelled one left the required check red and the PR unmergeable.

**Verified independently of those tests**, by raw SQL, real worker processes and real log output: see [INDEPENDENT_VERIFICATION.md](../08-testing/INDEPENDENT_VERIFICATION.md) — **12 PASS**, including the 55.4 r3 post-migration reproducibility gate. It has now found **six** defects the suite could not, among them `acks_late` set while crash recovery was an hour away, and a database error writing contract text into an operational log. That is *not* third-party verification and changes nothing about the statement below.

**Nothing is VERIFIED or PRODUCTION-READY.** `TESTED` means automated tests exist and pass locally; it does not mean behavior has been confirmed against the specification independently of those tests.

### Pending ratification

`IMPL-01` condition 4 leaves these explicitly unratified. None is locked; each is open to revision without amending anything.

The `review_assignments` and `escalations` tables and Review ownership were previously listed here under `IMPL-01` condition 3. All three were **ratified on 2026-08-17 by Amendment Batch AB-2** (`AM-22`, `AM-23`, `AM-24`) and are no longer pending.

| Item | What it is | Why it needs approval |
|---|---|---|
| `D-1` mapping threshold | An absent `confirm_threshold` refuses at publish time, with a second check at analysis time | An `ENG-09` conformance choice; the *value* still needs 35.10 calibration |
| `D-2` Mapping State persistence | Recorded in `evaluations.result.evaluated_facts`; both evaluators write it | `REC-03` calls the states "persisted" but no locked table carries them |
| `D-3` Requirement applicability | `company_standard_versions.configuration` → `{"applicability": …}`, failing closed to `REQUIRED` | Locked Step 28 lists "Required / Optional"; nothing sourced it |
| `D-4` `UNMATCHED_PROVISION` | The orchestrator writes no rows | `REC-02` defers persistence and surfacing |
| `M-2` mapping `AMBIGUOUS` semantics | Tied supporting clauses are `CONFIRMED` and all retained (Step 28 r2, 35.12); contradiction is caught by the conflict evaluator, producing `CONFLICT` / `DECISION_REQUIRED` | Interprets what locked Step 28's `AMBIGUOUS` means. **Consequence: nothing in V1 produces `MappingState.AMBIGUOUS`** — cross-Requirement ambiguity detection is unimplemented and no producer was invented |
| `tie_margin` | Unread since `M-2`; audited as unused and safe to remove, retained pending owner review | Removal is a configuration-shape change; evidence in [ANALYSIS_ORCHESTRATOR_GAP.md](../04-analysis-engine/EDGE_CASES/ANALYSIS_ORCHESTRATOR_GAP.md) §9 |
| `POST /reviews/{id}/analyze` permission | Mapped to `review.create` | Locked 49.3's table has no analysis row, though 49.8 and 49.10 presuppose the endpoint |
| Second-person approval mechanism | Co-signature within the append-only decision chain | Step 31 r15 permits the requirement without specifying the mechanism |

### Blocking the VERIFIED state

| ID | Item | Severity |
|---|---|---|
| **F-1** | EV-MIN was enforced `AFTER INSERT` on `findings` only, so deleting the last Evaluation orphaned the Finding undetected | ✅ **FIXED 2026-08-17** — migration `9c2f41ab77e3` adds `AFTER DELETE` and `AFTER UPDATE` constraint triggers on `evaluations`, both `DEFERRABLE INITIALLY DEFERRED`. 5 new invariant tests; the preflight verifies all three triggers exist. Migration round-trips twice cleanly |
| **F-3** | Mapping State (axis 1) has no column or enum | ✅ **ANSWERED by `D-2`** — the owner chose JSONB persistence in `evaluations.result.evaluated_facts`, written by **both** evaluators, so a replay can show what mapping concluded. Recorded under Pending ratification rather than left as a blocker |
| **F-4** | **Test suite non-determinism.** ⚙️ **Fixed and verified 2026-08-17** — see below |
| **F-6** | **A Legal Reviewer could reach no Review.** Visibility was ownership or an active `review_assignments` row, and no operation creates that row; "explicit Legal scope" (Step 24 r6) was named in two locked rules and defined in none, so **both** branches of r6 were unimplementable. The backend suite hid it — nine test sites insert the row with `db.add()`, so every Legal-workflow test ran against a state the product could not produce | ✅ **RESOLVED 2026-08-17 by `REC-09`** — Legal scope defined (non-withdrawn escalation **or** `LEGAL_REVIEW` status); `legal.review` holders may view, with no ownership and no decision authority. Implemented in `can_see_review` and the mirroring list scope; **no new permission, endpoint, table or schema change**. 15 new tests including a browser spec that cannot fake a database row. Per-user assignment (G1) **deferred to V2**; Contract/Document access unchanged and owner-only. Analysis: [LEGAL_ACCESS_GAP.md](../06-security/EDGE_CASES/LEGAL_ACCESS_GAP.md) |
| **45E** | Golden corpus is **73 fixture entries against 64 specified cases** — 21 `STRUCTURAL`, 9 `DOCUMENT_SUPPORTED`, 43 `STANDARD_DERIVED` — with **0 `NORMATIVE`** (counted from `backend/tests/corpus/*.json` on 2026-09-08; 67 = 21/9/37 on 2026-09-02, before AB-14 added six `CST-*` Constitution-value MATCH twins and re-expected the six live-paper fixtures as DEVIATION / UNABLE_TO_EVALUATE under `AM-43` r4). Standards are ratified **per document type** (2026-08-19), `MATCH` is asserted against both, and `L-01` closed under the per-type model. No fixture asserts any Rule Outcome but `NOT_APPLICABLE`, `load_fixture` refuses one that tries, and un-ruled deviations route to a human instead | Release gate — **blocked on real representative counterparty contracts** (rule 21: a `NORMATIVE` fixture needs supplied material, never manufactured). ⚠️ This cell read "blocked on an approved Legal Rule" until 2026-09-02, contradicting unit 10 of the Build state table in this same document: the zero-tolerance Legal Rule was approved 2026-08-19 and wired 2026-08-20, and is present in every ratified standard file. The Legal Rule is **not** outstanding |
| — | **No analysis calibration.** Mapping weights and thresholds are uncalibrated; locked 35.10 requires validation against a representative contract test set | Release gate |
| — | **Playwright not set up.** Locked Step 39/54 make it the browser-workflow tier | ✅ **DONE 2026-08-17** — 22 browser tests in [`frontend/e2e/`](../../frontend/e2e/), CI job 10. Scoped to what only a browser can prove: S-3's `HttpOnly` session cookie, `LEGAL-02` omission surviving to the page, 52.7's no-optimistic-UI rule including the 409 path, and SEC-02. **Supporting, not a locked tier and not a release gate** — locked 54.1 has no browser tier and 54.7's gate does not list one (see **C-12**) |
| — | **Analysis ran synchronously in the API.** Locked 55.1 makes it a worker job on the same image | ✅ **FIXED 2026-08-17** — `legalmind/worker/` dispatches to Celery when `LEGALMIND_BROKER_URL` is set (`202`) and runs inline when it is not (`201`), both through the same `run_analysis`. Dispatch performs no writes, so a lost message cannot strand a Review in `PROCESSING` — which Step 30 can only leave for `ANALYSIS_COMPLETE` or the terminal `ANALYSIS_FAILED`. The message carries identifiers only, and carries the dispatcher's evaluator-version fingerprint so a skewed worker refuses the job (55.1). The preflight fails a deployment with no broker. 23 tests plus a real Redis/Celery end-to-end run |
| — | **CI pipeline — no longer blocking.** [`.github/workflows/ci.yml`](../../.github/workflows/ci.yml) defines **14** jobs (13 as of 2026-08-25; job 14 — dependency and container scanning, A10 — added 2026-08-26) and `main` is protected on `3 · Authorization matrix (RELEASE-BLOCKING)`. `REC-08` (2026-08-17) locks **GitHub Actions** as the V1 CI/CD tooling, resolving **C-11**, so the workflow is an authorized use of the locked Step 39 stack rather than an unratified choice | ✅ Resolved — `REC-08` |
| — | **`NOT_IMPLEMENTED` is now empty.** `POST /reviews/{id}/export` left this row 2026-08-31 (the owner's export directive specified the formats 49.12 had left open — **PDF, DOCX**, decisions #231/#232 — IMPLEMENTED · TESTED, 7 tests incl. the LEGAL-02 omission-in-the-file property; `export.generate` granted alongside `report.view`, flagged for ratification). **`GET /auth/oidc/start` and `/auth/oidc/callback` left it 2026-09-01: IMPLEMENTED · TESTED** (28 tests, `tests/test_oidc.py`). No JWT/JWKS dependency was needed after all — the ID token is read from our own direct TLS exchange with the token endpoint and is never accepted from a client (OIDC Core §3.1.3.7 r6), so the flow is stdlib `urllib` only and rule 19 is never triggered. What remains is **not code**: the deployment's issuer, client id, client secret and redirect URI, which locked 55.6 makes a deployment prerequisite and the preflight reports as `BLOCKED` until set. Opened a second network egress path → **C-17** registered (rule 5) | `api/permission_map.py` `NOT_IMPLEMENTED` is `{}`; see [CONFLICTS.md](CONFLICTS.md) C-17 |

#### `F-4` — what was wrong, what was done, how it was checked

The *symptom* was real; the *recorded diagnosis* was not. Two checks disproved it:

* the suite runs green with `LEGALMIND_DATABASE_URL` pointed at a nonexistent host, so nothing in the test path opens the dev database — `api/deps.py`'s lazy engine is never constructed, because `get_db` is overridden by the harness;
* the actual mechanism was that every run shared the `public` schema and reset it with `DROP SCHEMA public CASCADE`. A backend left by an interrupted run held locks, so the reset raced and sometimes left a half-built schema. An intermediate fix using `pg_terminate_backend` then killed the *live* connections of any concurrent suite — `SSL connection has been closed unexpectedly` in whichever process lost. **The second failure mode was introduced by the fix for the first.**

Each run now migrates into a schema private to that process (`t_<epoch>_<random>`), points `search_path` at it for both its own connections and Alembic's, and drops only that schema at teardown. Nothing is terminated. A conservative sweep drops run schemas older than six hours, so debris from a crashed run cannot accumulate indefinitely while a live run — seconds old — is never a candidate. `test_each_axis_has_its_own_enum_type` was scoped to `current_schema()` in the same change; unscoped, it counted identically-named enum types belonging to other runs.

Verification, by concurrency rather than by assertion — the shared-schema design could not survive any of these. **The counts below are as of 2026-08-17 when the checks were run** (the suite was 385 tests then, 653 now); the property verified is stability under concurrency, not the number:

| Check | Result |
|---|---|
| Four concurrent full suites | 4 × `385 passed` |
| Staggered starts, so one run's setup overlaps another's teardown | 4 × `385 passed` |
| A run `kill -9`'d mid-suite, then a clean run | `385 passed` |
| Ten sequential runs | 10 × `385 passed` |
| Suite with the dev database URL pointed at a nonexistent host | `385 passed` |
| A run against a freshly emptied database | `385 passed` |

**24 consecutive clean runs, including 8 concurrent.** This is the author's verification, not an independent one: the row stays visible so CI or a reviewer can confirm it (`IMPL-01` condition 2). The run counts above are the suite size at the time of that verification; the suite has since grown to 446.

---

## Status vocabulary

These seven states describe *lifecycle*. They are a documentation convention only — they are **not** one of the five controlled legal-domain state axes in [DECISION_STATE_MODEL.md](../02-legal-domain/DECISION_STATE_MODEL.md) and must never share a field or enum with one.

| State | Meaning | Evidence required |
|---|---|---|
| **SPECIFIED** | The behavior is written down and internally consistent, but not settled — `PROVISIONAL` or `UNDER REVIEW` | A specification document |
| 🔒 **LOCKED** | Settled. Changing it requires explicit owner approval | An entry in [LOCKED_DECISIONS.md](LOCKED_DECISIONS.md) and a lock record in `all_lock.md` |
| **IMPLEMENTED** | Code exists that realizes the locked specification | Merged code, referencing the decision IDs it implements |
| **TESTED** | Automated tests cover it, including its golden-corpus fixtures where applicable | Passing tests in CI |
| **VERIFIED** | Behavior confirmed against the specification, not merely against the tests — the golden-corpus expectations hold and the explainability chain reconstructs | A recorded verification run |
| **BLOCKED** | Cannot advance until a named decision or dependency resolves | The blocking ID (`OD-*`, `C-*`, `N-*`) |
| **PRODUCTION-READY** | Verified, plus the deployment blockers register is clear | [STEP_55_DEPLOYMENT.md](../09-implementation/STEP_55_DEPLOYMENT.md) §55.6 |

Build state is reported in the **Build state** table above, which is the only place in this repository that asserts it. The table below reports **specification status only** — a row reading LOCKED there says the decision is settled, never that code exists for it.

---

## Current step

| | |
|---|---|
| **Steps 1–44** | 🔒 LOCKED |
| **Step 45A — LIABILITY-001** | 🔒 LOCKED |
| **`REC-01`–`REC-07`** | 🔒 LOCKED (reconciliation) |
| **`REC-08` — CI/CD tooling** | 🔒 LOCKED 2026-08-17 — GitHub Actions; resolves C-11 |
| **Amendment Batch AB-1** | 🔒 LOCKED |
| **Step 45B — Evaluator Data Contract** | 🔒 LOCKED (revised — `REC-05`, `REC-07`, AB-1) |
| **Step 45C — Liability Edge Cases** | 🔒 LOCKED |
| **Step 45D — Cross-Evaluator Edge Cases** | 🔒 LOCKED |
| **Step 45E — Golden Corpus** | ⏳ IN PROGRESS — [64 fixtures specified](../08-testing/GOLDEN_CORPUS_45E.md) |
| **Amendment Batch AB-2** | 🔒 LOCKED — `AM-22` `review_assignments`, `AM-23` `escalations`, `AM-24` Review ownership |
| **Implementation Readiness Gate** | ✅ [PASSED](../09-implementation/IMPLEMENTATION_READINESS_GATE.md) — all nine criteria met |
| **Implementation Authorization** | 🔒 LOCKED (`IMPL-01`, 2026-08-17) — retroactive, not backdated; see **Build state** above |


The master specification's closing position (`all_lock.md`, "Current position"):

> **The V1 specification is complete.** Remaining work is corpus authoring and implementation.

Two evaluators are specified: `LIABILITY-001` (`NUMERIC_COMPARISON`) and the generic `PRESENCE` evaluator. **No specific legal Requirement beyond `LIABILITY-001` is required by any locked decision.**

---

## Specification status by area

| Area | Status | Canonical documents |
|------|--------|---------------------|
| Product requirements & scope | LOCKED | [01-product/](../01-product/) |
| Roles & permissions | LOCKED | [USER_ROLES.md](../01-product/USER_ROLES.md) |
| Review lifecycle & workflow | LOCKED | [WORKFLOWS.md](../01-product/WORKFLOWS.md) |
| Legal analysis philosophy & AI boundary | LOCKED | [LEGAL_ANALYSIS_PHILOSOPHY.md](../02-legal-domain/LEGAL_ANALYSIS_PHILOSOPHY.md) |
| Company Standards & configuration versioning | LOCKED | [COMPANY_STANDARDS.md](../02-legal-domain/COMPANY_STANDARDS.md) |
| Legal Rules & clause library | LOCKED | [LEGAL_RULES.md](../02-legal-domain/LEGAL_RULES.md) |
| Finding classification | LOCKED — canonicalized (`REC-01`) | [FINDING_CLASSIFICATION.md](../02-legal-domain/FINDING_CLASSIFICATION.md) |
| **Decision State Model (five axes)** | LOCKED (`REC-06`) | [DECISION_STATE_MODEL.md](../02-legal-domain/DECISION_STATE_MODEL.md) |
| Legal Decisions & approval | LOCKED | [LEGAL_DECISIONS.md](../02-legal-domain/LEGAL_DECISIONS.md) |
| Document model & types | LOCKED | [DOCUMENT_MODEL.md](../03-document-model/DOCUMENT_MODEL.md) |
| Document versioning | LOCKED (Step 26) / **PROVISIONAL elaboration (Step 33, `REC-04`)** | [DOCUMENT_VERSIONING.md](../03-document-model/DOCUMENT_VERSIONING.md) |
| Evidence model | LOCKED | [EVIDENCE_MODEL.md](../03-document-model/EVIDENCE_MODEL.md) |
| Ingestion & processing pipeline | LOCKED | [PROCESSING_PIPELINE.md](../03-document-model/PROCESSING_PIPELINE.md) |
| Analysis engine architecture | LOCKED | [ANALYSIS_ENGINE.md](../04-analysis-engine/ANALYSIS_ENGINE.md) |
| Requirement mapping | LOCKED (**thresholds provisional**) | [REQUIREMENT_MAPPING.md](../04-analysis-engine/REQUIREMENT_MAPPING.md) |
| Fact extraction | LOCKED | [FACT_EXTRACTION.md](../04-analysis-engine/FACT_EXTRACTION.md) |
| Rule engine | LOCKED | [RULE_ENGINE.md](../04-analysis-engine/RULE_ENGINE.md) |
| Conflict detection | LOCKED | [CONFLICT_DETECTION.md](../04-analysis-engine/CONFLICT_DETECTION.md) |
| Explainability | LOCKED | [EXPLAINABILITY.md](../04-analysis-engine/EXPLAINABILITY.md) |
| System architecture & domains | LOCKED | [SYSTEM_ARCHITECTURE.md](../05-architecture/SYSTEM_ARCHITECTURE.md) |
| Technology stack | LOCKED (stack table only) | [BACKEND_ARCHITECTURE.md](../05-architecture/BACKEND_ARCHITECTURE.md) |
| Frontend architecture | 🔒 LOCKED (Step 52) | [FRONTEND_ARCHITECTURE.md](../05-architecture/FRONTEND_ARCHITECTURE.md) |
| API architecture | LOCKED (Step 43) + Step 49 specified | [API_ARCHITECTURE.md](../05-architecture/API_ARCHITECTURE.md) |
| Database domain model & schema | LOCKED | [DATABASE_ARCHITECTURE.md](../05-architecture/DATABASE_ARCHITECTURE.md) |
| Exact schema & ERD | LOCKED (+ AB-1, AB-2) | [DATABASE_MIGRATIONS.md](../09-implementation/DATABASE_MIGRATIONS.md) |
| Storage architecture | LOCKED (responsibilities) | [STORAGE_ARCHITECTURE.md](../05-architecture/STORAGE_ARCHITECTURE.md) |
| Authorization & ownership | LOCKED | [AUTHORIZATION.md](../06-security/AUTHORIZATION.md), [OWNERSHIP.md](../06-security/OWNERSHIP.md) |
| Security model | LOCKED (boundaries) | [SECURITY_MODEL.md](../06-security/SECURITY_MODEL.md) |
| Authentication | 🔒 LOCKED — OIDC primary, password fallback (OD-9), **session model amended by `AM-36` (AB-8, 2026-09-01)**: a stateless 24h JWT is permitted for the OIDC path, its authority claim is advisory and never enforced, and server-side sessions remain the only mechanism for password login. **All IMPLEMENTED · TESTED**; OIDC needs deployment IdP configuration (55.6), not code | [STEP_47_SECURITY_SPECIFICATION.md](../06-security/STEP_47_SECURITY_SPECIFICATION.md) |
| **Step 47 — Security/Authn/Authz** | 🔒 LOCKED | [STEP_47_SECURITY_SPECIFICATION.md](../06-security/STEP_47_SECURITY_SPECIFICATION.md) |
| **Step 49 — API Finalization** | 🔒 LOCKED | [STEP_49_API_FINALIZATION.md](../05-architecture/STEP_49_API_FINALIZATION.md) |
| **Step 52 — Frontend Architecture** | 🔒 LOCKED | [STEP_52_FRONTEND_ARCHITECTURE.md](../05-architecture/STEP_52_FRONTEND_ARCHITECTURE.md) |
| **Step 53 — Observability** | 🔒 LOCKED | [STEP_53_OBSERVABILITY.md](../09-implementation/STEP_53_OBSERVABILITY.md) |
| **Step 54 — Testing Strategy** | 🔒 LOCKED | [STEP_54_TESTING_STRATEGY.md](../08-testing/STEP_54_TESTING_STRATEGY.md) |
| **Step 55 — Deployment** | 🔒 LOCKED | [STEP_55_DEPLOYMENT.md](../09-implementation/STEP_55_DEPLOYMENT.md) |
| Audit trail | LOCKED | [AUDIT_TRAIL.md](../07-audit/AUDIT_TRAIL.md) |
| Reproducibility | LOCKED | [REPRODUCIBILITY.md](../07-audit/REPRODUCIBILITY.md) |
| Test strategy, golden corpus, regression | 🔒 LOCKED (Step 54) | [08-testing/](../08-testing/) |
| `LIABILITY-001` evaluator policy | LOCKED | [LIABILITY.md](../04-analysis-engine/EDGE_CASES/LIABILITY.md) |
| `LIABILITY-001` evaluator data contract | 🔒 LOCKED (revised, AB-1) | [LIABILITY_EVALUATOR_CONTRACT.md](../04-analysis-engine/EDGE_CASES/LIABILITY_EVALUATOR_CONTRACT.md) |
| `PRESENCE` generic evaluator | 🔒 LOCKED (Step 45D) | [PRESENCE_EVALUATOR.md](../04-analysis-engine/EDGE_CASES/PRESENCE_EVALUATOR.md) |
| Cross-evaluator structural contract | 🔒 LOCKED (Step 45D) | [EVALUATOR_EDGE_CASES.md](../04-analysis-engine/EVALUATOR_EDGE_CASES.md) |
| Liability edge cases | 🔒 LOCKED (Step 45C) | [LIABILITY_EDGE_CASES.md](../04-analysis-engine/EDGE_CASES/LIABILITY_EDGE_CASES.md) |
| Termination / Indemnification / Governing Law evaluators | **NOT YET SPECIFIED** | [EDGE_CASES/](../04-analysis-engine/EDGE_CASES/) |
| Deployment / infrastructure | 🔒 LOCKED (Step 55) | [09-implementation/](../09-implementation/) |

---

## Explicitly NOT YET SPECIFIED

Do not assume, infer, or invent any of the following. Each requires its own specification step and approval.

**Legal domain**
* Every requirement evaluator other than `LIABILITY-001` — including Termination, Indemnification, Governing Law
* Risk classification rules
* Regulatory reference workflow (DPDP Act, IT Act, GDPR, etc. as *applied* rules)
* Exact legal approval thresholds beyond those locked in `LIABILITY-001`

**Product**
* Exact customization workflow
* Export delivery beyond download (email summary — needs a mail dependency, rule 19; formats themselves were specified by the owner 2026-08-31: PDF, DOCX — built)
* UI/UX design

**Deliberately deferred by owner decision (2026-08-16)**
* The scoring-band → mapping-state mapping — how Step 35's `CANDIDATE-REVIEW` / `NOT MAPPED` / `NO_CONFIDENT_MAPPING` map onto Step 28's `CONFIRMED` / `AMBIGUOUS` / `UNRESOLVED`. **Do not infer it.**
* The shape and contents of `rule_configuration` (named in 45B.9, never specified)
* Persistence, surfacing, and review treatment of `UNMATCHED_PROVISION` observations
* The three Step 33 rules with no Step 26 counterpart (sequential version numbering, invalid/withdrawn semantics, predecessor chain)

**Technical**
* Authentication implementation, and integration with any existing authentication system or API
* Exact API endpoint naming (explicitly excluded from the Step 38.24 lock)
* `document_evidence.source_type` and `legal_decisions.decision_type` enum values ("finalized during implementation")
* Step 35 numerical scoring weights and thresholds (explicitly illustrative, pending a representative contract test set)
* Object-storage provider/layout, retention and encryption key management
* Deployment orchestration beyond the Step 39 recommended shape
* Implementation sequencing and milestones

---

## Conflicts

**C-01 through C-04 were reconciled on 2026-08-16** (`REC-01`–`REC-06`). None was a true contradiction. The finding-type enum is no longer blocked: the Step 36 seven-value set is canonical for Finding Classification, and `EXTRA`/`ADDITIONAL` became the document-level `UNMATCHED_PROVISION` observation.

**C-09 was resolved on 2026-08-17** by `IMPL-01` and Amendment Batch AB-2 — implementation authorized retroactively, with the lock record stating plainly that the work preceded the authorization; `review_assignments` and `escalations` ratified as `AM-22` and `AM-23`; Review ownership resolved by `AM-24`. The technical findings that review surfaced are **not** closed by it — they are tracked under **Blocking the VERIFIED state** above.

**C-11 was resolved on 2026-08-17** by `REC-08`: the Step 39 stack table's CI/CD row governs and GitHub Actions is the V1 tooling. Step 55.6's other NOT YET SPECIFIED line items — hosting platform, orchestration, object-storage provider, monitoring stack, DR objectives — are untouched by it.

Four low-severity items remain open (C-05–C-08), tracked in [CONFLICTS.md](CONFLICTS.md), along with **C-10** (MEDIUM — the `roles` seed list vs the canonical role matrix).

One item was **deliberately left unspecified**: the scoring-band → mapping-state mapping (C-02 sub-item). Do not infer it.


## Known limitations — current

*Kept here because this is the only document that may assert build state, and `HANDOFF.md`'s own
limitations section is frozen at 2026-08-18. Each entry names where the evidence lives.*

| Limitation | Evidence |
|---|---|
| **Ask retrieval recall is 0.891 against a measured 0.938 vector ceiling** (0.906 on a second run — the rescue judge makes this line non-deterministic by about one question; every blocking quantity is stable) (re-measured 2026-09-16 through the production path; the 0.625 it read before came from a gate that skipped routing, evidence rescue and the sufficiency screen). 3 of 64 answerable questions are refused for want of evidence and none is misrouted — the 7 that were went when `is_comparison_question` was rebuilt around a position reference the same day. Nothing answered has been wrong (faithfulness 1.0, 0 wrong answers reaching a user). The cause is `COSINE_FLOOR` applied at two points; the owner ruled 2026-09-14 that the dial moves only where recall improves without a rise in wrongly-answered. The query planner (Phase 1, 2026-09-17) was measured and does not move it — its reformulations cannot open the gate by design and moved ranking by about one question; the remaining gap is ranking inside a correctly retrieved set, i.e. the reranker's. | `tests/test_assist_retrieval_fusion.py` (xfail strict), `RETRIEVAL_RECALL_AUDIT_2026-09-02.md`, `tests/assist_eval/baseline.json` |
| **Ask is effectively unusable in Hindi and Hinglish, and its safety screens were weaker there** — measured 2026-09-15: a Devanagari comparison question was answered generatively instead of routing to the evaluator, and a Devanagari claim passed the grounding check vacuously. Fixed on `fix/language-safety-screens`, **merged as PR #53 and DEPLOYED 2026-09-16**. Retrieval itself remains marginal: Hinglish clears the 0.50 gate by as little as 0.009 and only because it carries English legal nouns; Devanagari scores 0.037. | [ASK_AI_PROGRAMME.md](ASK_AI_PROGRAMME.md) §D.5, `tests/test_assist_language_safety.py`, `RESEARCH_DOMAIN_C_RD_2026-09-04.md` §2 |
| **Faithfulness 1.0 is measured on the answerable half only** — the 21 false refusals never reach generation, so the figure is scored on 33 of 64 answerable questions, not all of them. | `tests/assist_eval/baseline.json` vs the 64/13 split |
| **A capability question ("what can LegalMind do?") searches the Constitution and returns unrelated standards.** No capability question shape exists; the POSITIONS fallback is unconditional. Needs `AM-68`. | [ASK_AI_PROGRAMME.md](ASK_AI_PROGRAMME.md) §C, `assist/routing.py` |
| **Multi-document conversations do not exist** — one contract per conversation, permanently; "compare document A with document B" is structurally impossible. A product decision (OD-A), not a defect. | `assist/service.py` `attach_contract`, `conversations.contract_id` |
| **The gate cannot catch adversarial near-misses** — topical-but-non-answering clauses score inside the answerable distribution for any model. The citation guardrail is the only defence. | `assist/calibration.py`, `assist/guardrails.py` docstrings |
| **Two evaluation families are unmeasured, deliberately** — paraphrase similarity and legal phrasing need human-written questions; inventing them would bias the result. | `tools/benchmark_retrieval.py` |
| **A document declares one type.** On a hybrid agreement, present clauses of every family are measured but absence is asserted only inside the declared family. | `APPLICABILITY.md` § Known limitations |
| **`AM-51` r5 is not implemented** — a type set after analysis does not re-run it. Mooted in practice by `AM-64`: the reader never selects a type. | C-21 |
| **Semantic recognition is model-dependent** — the same clause may be adjudicated differently across runs (observed once in 30 live cases). Every recognition records model, prompt version, payload hash and the verbatim span, and persisted Findings stay reproducible from what was recorded. | `AM-54` r6 |
| **Only 2 of 7 Constitution "Unacceptable Position" paragraphs are mechanically checkable** — the other five are qualitative. A deviation against those stays Needs review until Counsel gives a checkable rule. | `evaluation/constitution_boundaries.py` |
| **`NORMATIVE` golden-corpus fixtures are unauthored** — writing one would make a fabricated legal conclusion normative. | `tests/test_golden_corpus.py`, `corpus_coverage.json` |
| **Report export is not implemented** — formats are NOT YET SPECIFIED (49.12). | `BACKEND_FREEZE_HANDOFF.md` |
| **Two PO standards are drafted but deferred** — no Purchase Order exists to calibrate against (locked 35.10). Explicitly not a blocker (owner, 2026-09-14). | `company_standards/proposed/README.md` |
| **Illegible native text is never repaired, only OCR'd or refused** — the inversion that recovers body text corrupts every capital, which is the invented text 34.9 forbids. | `ingestion/parsing.py` |
| **Hosting, orchestration, object storage, monitoring and DR objectives remain NOT YET SPECIFIED** (55.6). | `DEPLOYMENT.md` § NOT YET SPECIFIED |
| **Production preflight: 17 PASS, 8 ATTEST, 0 FAIL, 0 BLOCKED** (live host, 2026-09-14). Every remaining row is an attestation about the platform the application cannot observe from inside itself. | [ops/production/README.md](../../ops/production/README.md) |
| **The egress allow-list is unproven** — it must be set and probed at the hosting provider's firewall, not from this host. The single-seam and payload screen are enforced in code regardless. | `AM-30` t8/t10 |
| **Backups exist and are restore-verified, but only on the machine they protect.** Off-host copies are outstanding. | `/var/backups/legalmind`, nightly 02:30, 14-day retention |
| **The deployment runs from `/root` as root**, which is why `ProtectHome` cannot be set. Isolation is otherwise in force (`NoNewPrivileges`, `PrivateTmp`, `ProtectSystem=full`, `PrivateDevices`, memory and task ceilings). Relocating to `/opt` under a dedicated user is a planned migration. | `10-hardening.conf` |
| **No Content-Security-Policy.** Next.js ships an inline bootstrap script, so a correct policy needs per-request nonces — a code change, not a header. The other five security headers are live. | nginx site config |
| **Malware scanning is `accepted-absent`**, decided on evidence: the real threat is parser exploitation, which a signature scanner does not catch, and that is mitigated by service isolation instead. A crafted document that exploits PyMuPDF or tesseract is still not detected before parsing. | `ops/production/README.md` |
