"""Analysis orchestration — locked 44.2 / 44.40.

The stage that joins the layers. Everything it calls was already built and tested;
this package adds no legal behaviour of its own and deliberately contains no
classification, no threshold and no rule.
"""
