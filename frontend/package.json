{
  "name": "legalmind-frontend",
  "private": true,
  "version": "0.1.0",
  "description": "LegalMind V1 frontend — locked Step 52. Next.js + TypeScript (Step 39). shadcn/ui + Tailwind approved for incremental adoption (owner, 2026-09-10) — see CLAUDE.md § UI and UX work for binding terms; no other technology without rule 19 approval.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "lint": "npm run typecheck && npm run check:terms",
    "check:terms": "node scripts/check-forbidden-terms.mjs",
    "test": "vitest run",
    "test:all": "npm run typecheck && npm run check:terms && npm run test",
    "test:e2e": "playwright test",
    "design-qa": "playwright test e2e/visual.spec.ts",
    "prebuild": "node scripts/guard-build-target.mjs",
    "deploy": "bash scripts/deploy-frontend.sh"
  },
  "dependencies": {
    "lucide-react": "^1.38.0",
    "next": "16.3.4",
    "react": "19.2.0",
    "react-dom": "19.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "24.10.1",
    "@types/react": "19.2.2",
    "@types/react-dom": "19.2.2",
    "typescript": "5.9.3",
    "vitest": "^5.0.0"
  }
}
